# Backlog — observations not actioned in the current task

Claude Code is required by `CLAUDE.md` Rule 5 to deposit observations here instead of acting on them mid-task. The user triages this list between phases.

## Format

```
- [YYYY-MM-DD][phase N] {one-line observation}
```

## Entries

<!-- Entries are appended below. Keep them one line each. -->
- [2026-05-12][phase 0] Deferred §J.0 task 14 (Vercel deploy). Will be picked up when there's a meaningful deployment to test, or as part of Phase 12 launch readiness. v1 Vercel project remains untouched for now.
- [2026-05-12][phase 1] API_FOOTBALL_KEY must be in .env.local before integration tests can run. User-managed (protected path).
- [2026-05-12][phase 2] Player seeding strategy: seed by SQUAD (GET /players/squads?team=) not by league enumeration. Squad-first is atomic and ~10% of daily quota. Lazy-fetch full player profiles only when needed.
- [2026-05-12][phase 2] Phase 2 player scope: Morocco men/women + all Botola clubs + WC 2026 48 nations + WAFCON 2026 ~12 nations + top-5 EU leagues (M+W). Estimated 6,000-7,000 unique players after deduplication.
- [2026-05-12][phase 2] Weekly cron job to refresh squads (transfer windows, new caps). Player stats refresh derives from /fixtures/players per played fixture, not /players bulk.
- [2026-05-12][phase 1 retrospective] Plan Part B had wrong API-Football league IDs (claimed Botola=322, real=200; Wydad=967, real=968; Raja=968, real=976; Coupe du Trône=670, real=822). Code now imports canonical IDs from src/lib/constants/canonical-ids.ts. Update Part B of plan to match verified IDs when convenient.
- [2026-05-12][phase 2] Première Ligue (France women's top flight) not surfaced in initial canonical-ID lookup. Discover via API during Phase 2 reference-data seed or in Phase 4 cleanup.
- [2026-05-12][phase 2] WC 2026 Qualification Africa (id=29) shows year=2023 in API. Investigate during seeding: may need different ID for the 2026 qualifier cycle, or API may not yet have populated the 2026 season.
- [2026-05-12][phase 2] Vary upgrade decision: stay on current plan (limit_day=7500) until Phase 2 seeding tells us if 7500 is enough. Upgrade to higher-tier (75k/day) only if real seeding hits the cap.
- [2026-05-12][phase 2.2 retro] drizzle-kit migrate does not auto-load .env.local. Future cron jobs or CI scripts must explicitly `set -a && source .env.local && set +a` before invoking drizzle-kit commands. Document in scripts/README.md or seed.ts header when those land.
- [2026-05-12][phase 2 retro] API-Football upgraded from Pro to Ultra. New daily limit: 75,000 requests/day (verify with /status). Previous concern about 7,500 limit is resolved. Phase 2 player seeding can run in single passes without throttling.
- [2026-05-12][phase 4] At Phase 4 start, read docs/research/atlaskings-v2-navigation-supplement.md and decide which Sofascore conventions to adopt: URL patterns, tab state via hash, slug ordering, breadcrumb structure, SWR refresh intervals, cache TTLs. Document decisions inline in the Phase 4 proposal.
- [2026-05-12][phase 5] Featured-match selection algorithm for homepage carousel. Sofascore uses (homeTeam.userCount + awayTeam.userCount) — we have no equivalent signal from API-Football. Default candidate: tier-based ranking (Tier 1 WC > Tier 2 Morocco > Tier 3 WAFCON > Tier 4 top-5 EU > Tier 5 other), with secondary sort by kickoff proximity (closest upcoming first). Decide at Phase 5 proposal time.
- [2026-05-12][phase 3+] Saudi Premier League Women (id=1227, season 2026 active) discovered during Tier 6 verification. Consider adding to VERIFIED_COMPETITIONS as Tier 5 (women's) addition.
- [2026-05-12][phase 2.4] Algeria Ligue 1 (186) and Tunisia Ligue 1 (202) have statistics_fixtures=false in coverage. Phase 8 UI must check coverage flags before rendering the Statistics tab for these leagues.
- [2026-05-12][phase 11+] Upstash Redis quota tracking deferred. Currently dead code (no-op when unconfigured). v2 scale (~600 calls/day) does not require it. Revisit if API-Football rate limiting becomes a real issue or if quota dashboard is needed for ops.
- [2026-05-12][phase 2.3 retro] Country-name-to-code resolution was missing across ingestion mappers (teams, venues). Caused FK violation on first real seed attempt. Fixed via buildCountryLookup() + resolveCountryCode() helper with alias map for API-Football quirks (England→GB-ENG, South-Korea→KR, etc). Future ingestion mappers MUST use this pattern for any country reference.
- [2026-05-12][phase 12] Optimize workers/live-poller/Dockerfile: add .dockerignore, multi-stage build, only copy what the worker actually needs from src/lib/. Current Dockerfile is functional but bloated for Railway production deployment.
- [2026-05-12][phase 4] shadcn style is "base-nova" (Base UI primitives), not the older "new-york" (Radix) preset referenced in some plan/research docs. Component APIs are similar but imports come from @base-ui/react. Document any deviations as encountered.
- [2026-05-13][phase 5] Live-poller worker must populate homeName/awayName in ScoreUpdatePayload when pushing to Pusher. Currently sends fixtureId + scores only. LiveTicker component falls back to "Team" when names are absent.

- [2026-05-13][phase 4.5] Visual richness pass. Sofascore visual analysis at docs/research/sofascore-analysis-atlas-kings-phase4-5.md is the authoritative reference. Three product decisions to lock before implementation: (a) LeftRail scope at current data state, (b) LiveTicker integration vs row-level live styling, (c) homepage hero philosophy. Target 8-12 writes across 2-3 sub-tasks.

- [2026-05-13][phase 6+] Inner-page 3-column shell — one reusable layout primitive used by competition/team/player/match-detail pages. Measurements at 1280px viewport: page gutter ~16px, left rail ~360-380px, gap ~16px, center column ~500-520px flexible, gap ~16px, right rail ~280-300px, page gutter ~16px (total ~1224px usable). Homepage explicitly does NOT use this shell — stays single-column. Current LeftRail.tsx at 280px is too narrow for what fits inside (featured match card with crests + score + kickoff, fixture rows with team names) — widen to ~360px when reintroduced, or rebuild entirely. Atlas Royal palette applied throughout.

- [2026-05-13][phase 6+] Right-rail editorial content (replaces Sofascore's ad slot since we don't run ads per Loi 09-08). Component library: TopScorersWidget, TrendingPlayersWidget, RecentResultsCarousel, H2HSummaryCard, MoroccoEditorialPicks, MeetTheTeamsCard (tournament pages), MoreMatchesTodayWidget. Each ~300px wide, ~250-400px tall, reusable across page types. This is genuine differentiation territory — Sofascore wastes this space on bet365 banners; we fill with useful content. Particularly valuable for Morocco editorial picks on WC 2026, AFCON, WAFCON team intro cards via API-Football CDN player photos.

- [2026-05-13][phase 6+] TournamentProgressBar component for cup/knockout competitions. Horizontal stage strip showing Group stage → R32 → R16 → QF → SF → 3rd → Final with dots/markers for completed and current stages. Applies to ~12 of 36 verified competitions: WC 2026 (id=1), AFCON (6), WAFCON (922), CAF Champions League (12), CAF Confed (20), Coupe du Trône (822), UCL (2), UEL (3), UECL (848), UWCL (525), UEFA Europa Cup Women (1191), AFCON Qualifiers (36). Driven by VERIFIED_COMPETITIONS metadata — add hasKnockout boolean flag per competition. Atlas Kings brand opportunity: use Morocco flag colors (red → gold → green) for the gradient where Sofascore uses generic red-orange-gold-green. Genuinely better visual story than Sofascore's blue-only palette.

- [2026-05-13][phase 6+] EditionSelector component for tournaments with multi-edition history. Horizontal carousel of past tournament years (2026 / 2022 / 2018 / 2014 / 2010...). Each edition links to a separate competition page with that year's data. Applies to WC, AFCON, WAFCON, UCL, UEL, UECL, Olympics Football (when added). Default selection is current edition.

- [2026-05-13][phase 6+] MatchesViewSwitcher segmented control with three modes: By date (chronological), By round (grouped by Round 1, 2, 3...), By group (grouped by Group A, B, C — tournament-only, hidden for leagues). Lives in left rail's Matches section on competition pages. Default By date for currently-active tournaments, By round for completed/upcoming league seasons.

- [2026-05-13][phase 6+] Trending page (/[locale]/trending) — master-detail UI distinct from standard inner-page shell. Left rail wider (~480px, not 360px) to fit fixture rows with full event metadata (sport, competition, country, team names, scores, kickoff times). Center shows selected event's detail card with own tabs (Details/Box score/Statistics/Commentary/Games/Media). Right rail editorial content. Clicking left items updates center without full page navigation (Next.js parallel routes or client-side state). Atlas Kings curation favors Morocco-relevant events: Botola Pro live matches, Atlas Lions fixtures, Wydad/Raja/FAR results, Hakimi/Ziyech/Mazraoui appearances.

- [2026-05-13][phase 6+] Layout matrix by page type (canonical reference for inner-page rendering):
  * Homepage: single column, no rails
  * Competition (League) e.g., Botola Pro, Premier League: left rail (Featured match card + Matches list with round selector) + center (Standings table with All/Home/Away tabs, then Stats/Details/Media tabs) + right rail (editorial)
  * Competition (Cup/Tournament) e.g., WC 2026, Coupe du Trône, AFCON: same as league + TournamentProgressBar above tabs + EditionSelector + MatchesViewSwitcher in left rail
  * Team: left rail (Featured match + Recent form) + center (Details/Matches/Standings/Squad/Top players/Statistics tabs) + right rail (top players, trending)
  * Player: left rail (career summary + current club) + center (Details/Season/Matches/Career tabs) + right rail (similar players, news)
  * Match Detail: left rail (lineups for one team) + center (Details/Odds-replaced/Lineups/Matches/Standings-or-Knockout/Media tabs) + right rail (H2H summary, predictions, more matches today)

- [2026-05-13][phase 4.5+ typography] Switch Latin face from Inter to IBM Plex Sans for deliberate pairing with IBM Plex Sans Arabic (same designer, same studio, intentional system — Sofascore can't match this because they don't prioritize Arabic). Add Recoleta or Cormorant as display face for editorial moments (page titles on team/competition pages, match detail headers, hero text). Recoleta is paid via Adobe — verify Adobe Fonts subscription or use free alternative (Cormorant from Google Fonts, Newsreader display variant, or Fraunces). Apply Level 1 patterns rigorously: tabular numerals via font-variant-numeric: tabular-nums on all scores/times/ages, tight line-height 1.2-1.3 on data-dense components, weight scale 400 body / 500 labels / 600 active+headings / 700+ display only, smaller body size 13-14px for data-dense UI vs 16px default. Implementation: Phase 4.5b (after current Phase 4.5a chrome polish completes) — update next/font/google imports in src/app/[locale]/layout.tsx, update --font-* CSS variables in tokens.css, retrofit existing components to use semantic weight scale.

- [2026-05-13][phase 4.5+ typography DECISION LOCKED] Display face = Fraunces (Google Fonts, variable, free). Pairs with IBM Plex Sans for editorial display moments: page titles on team/competition/player pages, match detail headers, hero text. NOT used for body/UI labels/data display. Body = IBM Plex Sans Latin (pairs with IBM Plex Sans Arabic). Weight scale: Fraunces 400-700 display, IBM Plex Sans 400/500/600/700 semantic (body/labels/active+headings/strong-emphasis). Variable axes: weight + opsz + soft. Implementation in Phase 4.5b after current chrome polish 4.5a ships.
- [2026-05-14][phase 5] team.md Stats tab (Block 1 — Attaque) includes "Grosses occasions par match" (big chances per match — aggregate). Verify during Phase 5 data ingestion that API-Football /teams/statistics actually returns this field for covered leagues. If absent or unreliable, drop from Block 1 before Phase 8 team-page Stats tab UI build. Distinct from per-shot big-chance attribution (Sofascore proprietary) which is already cut. Surfaced by 2026-05-14 schematic audit pass over competition-cup.md, team.md, player.md, match.md — that audit otherwise found zero false-positive specs of unavailable features.
- [2026-05-14][phase 5+] Page 2 (competition-league.md) Standings Tracker chart retrofitted out of v1 scope on 2026-05-14. API-Football /standings endpoint provides only current-snapshot standings — no per-round historical archive — so a Tracker visualization is not buildable from API data alone. Slot in competition-league.md now marked "reserved, replacement TBD post-Phase-5 data ingestion." Revisit post-Phase-5 once we have a populated fixtures table: per-round standings can be reconstructed by replaying finished fixtures in date order, which would unlock a Tracker rebuild as a derived dataset. Decision and replacement-feature design owed at that point. Commit: docs(schematic): retrofit Page 2 Standings Tracker as deferred slot.
- [2026-05-14][phase 6+] [SUPERSEDES 2026-05-13 layout matrix entry] The 2026-05-13 "Layout matrix by page type (canonical reference for inner-page rendering)" entry above is superseded as of 2026-05-14. Each page's layout is now canonically specced in its own schematic document under docs/schematics/ (homepage.md, competition-league.md, competition-cup.md, team.md, player.md, match.md). The matrix bullet list in the older entry is retained for history but is no longer the source of truth. Note: other 2026-05-13 [phase 6+] entries (inner-page 3-column shell, right-rail editorial components, TournamentProgressBar, EditionSelector, MatchesViewSwitcher) remain canonical — schematics reference them as the source spec.
- [2026-05-14][phase 6] Plan deviation: Phase 6 (Competition page) reordered before Phase 5 (Homepage). Rationale: homepage is a structural singleton (single column, no inner-page shell) and cannot serve as the canonical visual reference for the rest of the site. competition-cup.md (WC 2026 instance) selected as the first build target because (a) it is the superset of competition-league.md (inherits the 3-column inner-page shell + adds cup-specific components: TournamentProgressBar, EditionSelector, MatchesViewSwitcher, knockout bracket), (b) WC 2026 is the launch-day content priority per Part N of the plan, (c) WC 2026 data is fully seeded per Phase 2 exit criteria (104 fixtures, 48 teams, 12 groups). Architectural rule for this build: every cup-specific UI component must be self-contained so competition-league.md can instantiate the same shell without rendering them. Known constraint: WC 2026 is pre-tournament (kicks off 11 June 2026), so visual review validates the pre-tournament/scheduled state only — populated/live/post-tournament states reviewable after matches play. Phase 5 (Homepage) reverts to canonical order after Phase 6 ships. Phase 4.5a chrome work (SportNav + Footer + i18n) retained on disk uncommitted; will be absorbed and revised inside the Phase 6 WC 2026 build rather than reverted or committed separately.
- [2026-05-14][phase 6+] getFeaturedMatch in queries.ts takes moroccoTeamId as a required param, coupling it to a Morocco-first selection strategy. When competition-league.md is instantiated for Botola Pro, the featured match algorithm needs different logic (Wydad/Raja next match, or stakes-weighted selection). Generalize into a strategy pattern or discriminated config param in Phase 6 league sub-task.
- [2026-05-14][phase 6 sub-task 6.1 retro] Three corrections owed from Sub-task 6.1 implementation: (a) BACKLOG L54 typography decision wording says "page titles" for Fraunces display face — this means About-card section titles, NOT H1 (H1 uses IBM Plex Sans per competition-cup.md Section 2). Update L54 wording when convenient. (b) SportNav.tsx was deleted outright in Sub-task 6.1 rather than "absorbed and revised" as the Phase 6 plan-deviation entry (L63) stated — the single-sport nav concept was folded into the Topbar, not preserved as a separate component. (c) Typography swap (Inter to IBM Plex Sans + Fraunces) shipped inside Phase 6 Sub-task 6.1 rather than as a standalone Phase 4.5b task — the swap was a prerequisite for competition page chrome and could not be deferred further.
- [2026-05-14][phase 6 sub-task 6.5] Rewrite AdaptiveTopStrip from static "WC 2026 group preview + countdown" to a dynamic live/upcoming match ticker. Driven by DB, not static config or editorial curation. Behavior: query fixtures across ALL tracked competitions; if any matches are status=live now, show those; if zero live, show the next 15 upcoming fixtures by kickoff time ASC. Horizontal scroll handles overflow. Replace current WC 2026 countdown (drop entirely — countdown was useful pre-tournament but doesn't generalize). Same component renders on every page, no context-awareness. Scope of competitions = all 36 verified competitions seeded per Phase 2. Sub-task 6.5 follows 6.4 (right rail) in Phase 6 sequence — must ship before Phase 5 (homepage rebuild) because homepage depends on the AdaptiveTopStrip. Schematic deviation from homepage.md §1 (which speccs 4 modes including the countdown). Document deviation in the eventual implementation BACKLOG retro.
- [2026-05-14][phase 6 sub-task 6.3] Best 3rd-placed teams tab: FIFA full tiebreaker chain requires two unseeded data sources. (a) Fair-play tally (yellow + red cards per team aggregated from fixture events) — derivable post-Phase 5 ingestion once events data is populated. (b) FIFA Men's World Ranking — no API-Football endpoint exists; requires external data source, manual config table, or Phase 12+ admin tooling. Both are also needed for FifaRankingBadge (stubbed with null). Current Best 3rd sort falls back to stable order when tied through goalsFor; ⓘ disclaimer tooltip surfaces this to the user.
- [2026-05-14][phase 6 sub-task 6.3] CenterTabs hashchange listener missing: MiniStandingsPreview dispatches HashChangeEvent via window.dispatchEvent(new HashChangeEvent('hashchange')) but CenterTabs only reads hash on mount (useEffect with [tabs] dep). The "View all groups" button sets the hash but doesn't trigger tab switch within SPA navigation. Fix: add a hashchange event listener in CenterTabs useEffect. Low priority — only affects in-page cross-tab navigation from Overview → Standings.
- [2026-05-14][phase 6 sub-task 6.3 retro] Sub-task 6.3 (Center column tabs) implementation notes and deviations: (a) Tab content H2 ("Overview — World Cup 2026") removed — redundant with page H1 + active tab label. Schematic implied per-tab section heading; deviation acceptable. (b) FeaturedMatchCard footer simplified — removed "Group Stage - 1" (redundant with page context) and "FIFA: — vs FIFA: —" (rank data not seeded). Added stadium/city line from hydrated venue field. (c) Knockout bracket rendered as vertical list per phase, not converging CSS bracket. Visual bracket deferred — pre-tournament data is identical placeholders across all slots, list communicates as clearly. Build proper bracket when real knockout data exists OR as discrete v1+ polish task. (d) MiniStandingsPreview groupLabel normalization at query layer ("Group A" to "A") to match metadata labels. Pattern to follow for future joins. (e) HistoricalWinners ordinal handling via formatOrdinal() helper — produces 1st/2nd/3rd (EN), 1er/2e/3e (FR), plain numbers (AR). Use this helper for any future ordinal-in-text rendering. (f) Next.js 16 dynamic route params asymmetry: page components receive percent-encoded values, generateMetadata receives decoded values. Non-ASCII slugs (Arabic) fail comparison in page components without explicit decodeURIComponent. Fix applied to CompetitionPage + 3 placeholder routes (equipe, joueur, match). Pattern for any future dynamic route with non-ASCII segments: decodeURIComponent at the top of the page component before any string comparison or display. (g) Best 3rd-Placed Teams tab added as a 4th tab between Standings and Knockout, gated by metadata.hasBestThirdPlace flag in CupMetadata. FIFA tiebreaker chain partially implemented (points DESC, goalDiff DESC, goalsFor DESC). Fair-play tally and FIFA World Ranking tiebreakers fall through to stable sort — fair-play data needs Phase 5+ ingestion derivation from event data, FIFA Ranking needs external data source (no API-Football endpoint, Phase 12+ admin tooling or manual config table). Conditional disclaimer renders only when ties beyond GF actually exist in visible data. (h) Arabic team names not populated in team.name['ar'] jsonb for most teams — falls through to English. Same data issue as Scotland from 6.2 retro. Phase 5+ data ingestion should backfill name['ar'] across all 48 WC 2026 teams and all teams used in MoroccoContextLine / FixtureRow / standings tables. Tracked here so it is not lost.
- [2026-05-15][phase 6 sub-task 6.4 retro] Sub-task 6.4 (Right rail + Videos + About + FAQ + SEO + Share primitives) implementation: (a) Right rail widgets shipped: TopScorers, TopAssists, MeetTheTeams (Group C teams), MoreMatchesToday, NewsletterCard (reused from left rail). TopScorers/TopAssists/MoreMatchesToday render compact empty state pre-tournament — single line of explanatory text, no share button, share appears automatically when data populates. (b) Videos section as full-width section below the 3-column grid. Empty state until YouTube IDs are curated in Phase 9. (c) About card with 6 H2 sections + FAQ (collapsible details). Content sourced from competition-cup.md §11 — EN/AR drafted from FR with REVIEW comments. User polish post-commit. (d) SportsEventJsonLd component renders application/ld+json structured data for SEO. Per-fixture SportsEvent deferred to Phase 8. (e) Standings refactor: StandingsTab and MiniStandingsPreview changed from single container card holding 12 stacked tables to 12 separate cards per group, each with id="group-x" and its own share button. Schematic deviation; enables per-card sharing per the Phase 7+ shareable-cards-architecture initiative. (f) ShareButton (src/components/shared/ShareButton.tsx) and HashScrollHighlight (src/components/shared/HashScrollHighlight.tsx) generic primitives shipped. Used by WC 2026 now, reusable for any tournament/league/team/player/match page going forward. (g) Hash-fragment deep linking with Latin keys regardless of locale (e.g. #group-c, #meet-teams). Cross-locale share compatibility — a link shared from /fr lands correctly on /ar. (h) ShareButton hides when card is in empty-state (TopScorers, TopAssists, MoreMatchesToday, Videos). Reappears when data populates. (i) MeetTheTeams renders flag + team name only — captain photos deferred until squad data is hydrated in Phase 9+. (j) Sub-task 6.4 ships the v1 of the shareable-cards-architecture initiative (hash-fragment deep links). Full per-card URL refactor with OG images and per-card JSON-LD follows in Phase 7+.
- [2026-05-14][phase 6 sub-task 6.6] Knockout bracket visualization. Current Sub-task 6.3 implementation renders knockout phases as a vertical list of match cells per phase (R32 / R16 / QF / SF / 3rd / Final), accessed via phase selector chips. This communicates structure but not progression — fans cannot see which winner of Match X plays the winner of Match Y in the next round. Build a proper converging tournament bracket. Desktop: classic symmetric layout — left side renders 8 Round of 16 matches converging rightward, right side renders 8 Round of 16 matches converging leftward, Quarter-finals on both inner sides, Semi-finals, then Final at center. Round of 32 on far outer columns. Connecting lines drawn between adjacent matches and their successor cell. Mobile (~380px viewport): full bracket does not fit. Render as horizontal swipe between rounds — R32 first card, swipe to R16, swipe to QF, swipe to SF, swipe to Final. Each round shows its matches stacked vertically. No connecting lines on mobile (clutter). Tablet (~768px): degraded desktop bracket with smaller text and tighter spacing, or fall through to mobile pattern at threshold. RTL: full bracket mirrors — left-side rounds become right-side rounds in Arabic. Pre-tournament state: bracket renders with placeholder labels ("1st Group A" / "2nd Group B" per FIFA seeding rules) and connecting lines draw between empty cells to show the structural flow. Once group stage plays out, real team names populate. Replace the current PhaseSelector + KnockoutMatchCell list pattern with a new KnockoutBracket component. Keep PhaseSelector as a fallback for mobile (alternate to swipe if needed). Sub-task 6.6 follows 6.5 (AdaptiveTopStrip ticker) in the Phase 6 sequence — completes the WC 2026 competition page polish before Phase 5 homepage rebuild.
