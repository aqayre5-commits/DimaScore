# Backlog — observations not actioned in the current task

Claude Code is required by `CLAUDE.md` Rule 5 to deposit observations here instead of acting on them mid-task. The user triages this list between phases.

## Format

```
- [YYYY-MM-DD][phase N] {one-line observation}
```

## Entries

- [2026-06-14][phase 13 — standings] Durable root fix for the duplicate-standings-rows class (now patched at the read layer in ~6 surfaces via `dedupeStandingsByTeam` + `getStandings`/`getLiveGroupStandings` trim). The `standings` table accumulates duplicate rows per team under variant `group_label`s from `syncStandings`/the API: trailing whitespace (`Serie A `), two names for one league (Botola `Botola Pro` + `Botola Pro D1`), and the WC prefix/catch-all mess. Fix at ingest: trim + canonicalise `group_label` in `syncStandings`, and one-off delete the existing duplicate rows. Caveat introduced by the read-layer fix: `dedupeStandingsByTeam` on the flat **league** standings/teams collapses genuine split-format leagues (id 233 "Premier League", Serie A Women: Regular/Championship/Relegation rounds) to a single table (first row per team). Not the displayed EPL (id 39); revisit if a split-format league becomes prominent — it'd need a group-aware standings tab rather than a flat one.
- [2026-06-14][phase 13 — events] Two residual gaps after re-ordering `getFixturesMissingDetails` to newest-first: (a) finished fixtures that were detail-synced once but came back with **0 events** (e.g. a scored match with no goal events) are never retried — `detailsSyncedAt` is set so they fall out of `getFixturesMissingDetails`; add a periodic sweep that re-syncs finished fixtures with `detailsSyncedAt` set but no `fixture_events` (or where event count looks too low for the scoreline). (b) Upstream truth: API-Football's `/topscorers` is unreliable for Botola Pro 2025 (returns ~13 sparse rows, top scorer absent) — `fixture_events` is the source of truth there, which is why league/cup scorers fall back to events; the official aggregate can't be trusted to refresh it.

- [2026-06-14][phase 13 — scorers] Root cause behind the events-fallback: the `top-scorers` ingestion (`/api/cron/top-scorers` → `player_season_stats`) failed to populate **Botola Pro 2025** — 13 rows / 4 scorers / max 3 goals, while `fixture_events` holds 372 real goals over 192 finished matches. League + generic-cup pages now self-heal via `getResolvedTopScorers/Assists` (official aggregate when healthy, else events). Still to do: (a) re-run / fix the top-scorers cron for the current season at source; (b) the homepage rail scorers (`getRightRailTopScorers`) still read the raw `player_season_stats` across priority comps — a multi-competition query, so the per-comp fallback doesn't drop in cleanly; give it the same events-fallback treatment.

- [2026-06-14][phase 13 — scorers] `player_season_stats` is unreliable for short-edition tournaments: API-Football's /players/topscorers for competition 1 (World Cup) season 2026 returned non-participants (Algeria/Comoros/Burkina players, 10 goals) plus HTML-entity names (`&apos;`). The WC/cup `render-cup-page` rail now computes from this edition's goal events instead (`getEditionTopScorers`/`getEditionTopAssists`). Still on the polluted `player_season_stats` source: (a) the generic-cup renderer (`render-generic-cup-page` via `getTopScorersForLeague`/`getTopAssistsForLeague`) — switch to edition-based if those cups show the same garbage; (b) the homepage rail scorers (`getRightRailTopScorers`) which mixes priority comps incl. cups; (c) team-page `getTeamTournamentScorers`. Evaluate each. Root option: fix the top-scorers ingestion to not trust the endpoint for cup competitions.

- [2026-06-14][phase 13 — rail] Extract a shared `TopPlayersList` component. The ranked player-row markup (rank · photo · name link · team · stat) is now duplicated in `LeagueRightRail`'s private `TopPlayersList` (league rail) and the tournament `TopScorersWidget`/`TopAssistsWidget` (cup/WC rail). Factor one shared presentational component (props: title, players, statKey, locale) and use it in all three.

- [2026-06-14][phase 13 — standings] Consolidate the live-standings overlay math. `src/lib/standings/overlay-live.ts` (`overlayLiveStandings` + its `applyResult`) is now the shared util used by the cup/WC `StandingsTab`. The homepage rail's `HomeLiveGroupStandings.computeProvisional` (+ its own `applyResult`) still has a near-duplicate; migrate it onto `overlayLiveStandings` so the live-overlay math lives in one place. `compute.ts`'s server-side `applyOne` is a related but distinct (from-scratch, not overlay) copy — evaluate sharing the per-result math too.

- [2026-06-14][phase 13 — WC audit] Knockout bracket (`buildDynamicBracket`) hardening, DORMANT until 2026 WC knockout fixtures populate (currently 0 in DB; WC falls back to the static `wc2026-bracket-builder` so nothing is broken now). Three issues to fix before the round of 32: (1) `buildTies` pairs reversed home/away as a two-leg aggregate — WC is single-leg; pass an `isSingleLeg` flag so legs are never paired (low practical risk since it runs per-phase, but a latent correctness trap). (2) Knockout fixtures created with placeholder/null teams render as `? vs ?` in the dynamic builder (the static bracket shows proper "Winner Group A" slot labels); add TBD-team fallback labels. (3) Verify R32 (16-tie / 8-per-side) grid sizing in `computeGridConfig` vs the bracket component's row assumptions when only R32 fixtures exist. Also the already-noted live-minute-on-bracket-node item.

- [2026-06-14][phase 13 — WC audit] Morocco isn't flagged as its group in the WC 2026 metadata (`tournament-metadata.ts`, all groups `isMoroccoGroup: false`), so the WC page header shows no Morocco highlight. Morocco is in Group C. Polish: mark Group C `isMoroccoGroup: true` for 2026.

<!-- Entries are appended below. Keep them one line each. -->
- [CLOSED 2026-06-29][phase 13 — scorers] Audit other surfaces that compute scorers from `fixture_events` for the same Missed-Penalty bug just fixed in `getEditionTopScorers`/`getEditionTopAssists`. Audit verified all events-aggregation surfaces filter on `detail IN ('Normal Goal','Penalty')`; the Match API endpoint uses the negative `!detail.includes('missed')` filter (also safe). No remaining surface.
- [2026-06-29][phase 13 — flags] 18 team-logo `<Image>` sites bypass `getTeamFlagUrl()` and render raw `team.logoUrl`. WAFCON placeholder teams (logoUrl=null) show gray boxes on these surfaces: HomeLiveGroupStandings:204, HomeStandingsMini:68, StandingsWidget:61, CupOverviewTab:76, KnockoutMatchCard:115, CupGroupsSummary:59, FixtureRow:161/243, LeagueStandingsTab:133, LeagueRightRail:192, TeamCompetitionTeams:56/58, SearchModal:206/208, TickerStrip:64/65, WCFixturesTab:462, CupFixturesTab:333, TeamStandingsSection:94, TeamPageHeader:38/40/55/57, MatchInstantFallback:101. Plus CoachCareerTable:52/54 uses `.logo` field (normalize to `.logoUrl`). Tracked as Phase 13 task T1.
- [2026-06-29][phase 13 — coverage] Rule 12 violations confirmed in code audit: (a) render-league-page.tsx:302/312 passes top-scorers to LeagueRightRailCard without coverage.topScorers check — Algeria L1 (186) and Tunisia L1 (202) render empty cards in production; (b) equipe/[...slug]/page.tsx:276 always includes TeamStatistics tab without coverage.statisticsFixtures check — same leagues' teams show empty Stats tab. Tracked as Phase 13 tasks T2 + T3.
- [2026-06-29][phase 13 — scorers] getRightRailTopScorers (right-rail.ts:801-881) bypasses the events-fallback that getResolvedTopScorers uses. Botola Pro 2025 case confirmed: homepage rail / edition-maroc rail shows player_season_stats (sparse) while Botola page shows events-derived ~5 scorers from 372 goals. Fix sketch: dual-source SQL union in one query. Deferred to Phase 14.
- [2026-06-29][phase 13 — status] LIVE_CODES_ARRAY canonical but INT missing from two places: LiveScoreDisplay.tsx:8-10 (hardcoded subset) and schema.ts:268 partial-index WHERE clause. Interrupted matches don't render the live pill and don't hit the index. Tracked as Phase 13 task T6 (5-line fix + 1 migration).
- [2026-06-29][phase 13 — status] SCORED_STATUSES vs FINISHED_STATUSES drift. homepage.ts:55 and right-rail.ts:30 use `['FT','AET','PEN','WO','AWD']` inline (legitimate inclusion of WO/AWD for standings scoring) but the intent is undocumented. Extract as SCORED_STATUSES_ARRAY in match-status.ts. Tracked as Phase 13 task T7.
- [2026-06-29][phase 13 — security] match/[id]/page.tsx:54-57 parseFixtureId validates Number.isFinite + >0 but no upper bound. Pathological IDs like 999999999999999 reach the DB. Add `< 2_000_000_000` check. Tracked as Phase 13 task T8.
- [2026-06-29][phase 13 — security] In-memory rate limiting in api/admin/auth/route.ts:12-23 and api/v1/media/oembed/route.ts:4-17 is ineffective on serverless (per-invocation Map reset). Move to Upstash Redis. Deferred to Phase 14.
- [2026-06-29][phase 13 — live data] 4 surfaces still SSR-snapshot only and visibly stale during live matches: CupFixturesTab (AFCON/WAFCON/CDT), HomeTodaysMatches (homepage rail), MatchRow/MatchesThisWeek (homepage tabs), LeagueFixturesCard (league pages). Same pattern as today's WCFixturesTab fix. T4 + T5 cover the highest-impact two (HomeTodaysMatches + CupFixturesTab); rest deferred to Phase 14.
- [2026-06-29][bracket design] AFCON and WAFCON share one bracket design (different from WC); WC uses its own mirrored symmetric design. NOT a parity regression — intentional. Future audits should skip "bracket asymmetry between WC and CAF cups" as a finding.
- [2026-06-29][dedup] Confirmed 3+ near-identical copies: applyResult logic (overlay-live.ts:13, HomeLiveGroupStandings.tsx:51, standings/compute.ts:17); PlayerPhotoCircle markup (LeagueRightRail:42-54, TopScorersWidget:38-50, TopAssistsWidget:38-50, LeagueStatsTab, tournament/RightRail); TopPlayersList vs TopScorersWidget vs TopAssistsWidget have ~95% markup overlap (only diff is statKey). Extraction candidates for Phase 14 cleanup; not urgent because none diverge in behavior today.
- [2026-06-29][closed — re-audit] Verified resolved during 2026-06-29 audit, no Phase 13 action needed: (a) Missed-Penalty bug fully fixed everywhere events are aggregated; (b) cacheComponents migration complete (all 4 routes — homepage, match, team, competition — use `'use cache'` + `cacheLife(...)`; the older BACKLOG note claiming this was blocked by `revalidate` exports is stale); (c) finalize-on-absence covered by `/api/cron/finalize-stale` (5-min staleness window, acceptable); (d) no N+1 patterns or serial Promise chains in hot paths; (e) no XSS / SQLi vectors (Drizzle param + React auto-escape + escapeLikePattern on search); (f) admin session crypto solid (timing-safe HMAC, allowlist, httpOnly cookies).
- [2026-06-26][phase 13 — bracket] Backlog L24 items partially mooted for WC by `buildWc2026BracketWithLive` (WC now skips `buildDynamicBracket` entirely): single-leg pairing trap, TBD-team labels, R32-only grid sizing all still apply to AFCON (6) and WAFCON (922) which still use the dynamic builder. Re-evaluate if those brackets show structural oddities once their knockout rounds populate. Separately: the WC overlay treats current group standings as the source of truth for slot resolution — once R32 starts, standings are frozen, so this is stable; but if standings change retroactively (e.g. CAS ruling), the overlay would re-resolve and could orphan a fixture from its slot. Acceptable risk; flag if it ever surfaces.
- [2026-06-26][phase 13 — WAFCON 2026] Manual seed from CAF PDF landed (34 fixtures, 16 teams incl. 7 placeholders with ids −1..−7, 16 standings rows, 8 canonical knockout fixtures + 2 WC-Qualifying Play-offs). Swap to API-Football data once they publish WAFCON 2026 fixtures: `DELETE FROM fixtures WHERE id BETWEEN -1034 AND -1001; DELETE FROM standings WHERE competition_id=922 AND season_year=2026; DELETE FROM teams WHERE id BETWEEN -7 AND -1;` then `pnpm tsx --env-file=.env.local scripts/seed-wafcon-2026.ts`. Recheck weekly until non-zero counts come back.
- [2026-06-26][phase 13 — countries] `countries` table missing the `CV` (Cape Verde) row, so the men's `Cape Verde Islands` team and the new `Cape Verde W` placeholder both carry `country_code=null`. Add the row and a small backfill that resets `country_code='CV'` on those teams. Tiny but blocks proper country lookups (flag rendering, country filters).
- [2026-06-26][phase 13 — WAFCON 2026] Manual seed dropped venue info — PDF gives venue names ("Moulay El Hassan Stadium-Rabat", etc.) but our `venues` table is keyed on the API-Football venue id, which we don't have. All 34 fixtures inserted with `venue_id=null`. Restored by the API-Football swap; if WAFCON pages need venue names sooner, seed venues from the PDF first and link them.
- [2026-05-12][phase 0] Deferred §J.0 task 14 (Vercel deploy). Will be picked up when there's a meaningful deployment to test, or as part of Phase 12 launch readiness. v1 Vercel project remains untouched for now.
- [2026-05-12][phase 1] API_FOOTBALL_KEY must be in .env.local before integration tests can run. User-managed (protected path).
- [2026-05-12][phase 2] Player seeding strategy: seed by SQUAD (GET /players/squads?team=) not by league enumeration. Squad-first is atomic and ~10% of daily quota. Lazy-fetch full player profiles only when needed.
- [2026-05-12][phase 2] Phase 2 player scope: Morocco men/women + all Botola clubs + WC 2026 48 nations + WAFCON 2026 ~12 nations + top-5 EU leagues (M+W). Estimated 6,000-7,000 unique players after deduplication.
- [2026-05-12][phase 2] Weekly cron job to refresh squads (transfer windows, new caps). Player stats refresh derives from /fixtures/players per played fixture, not /players bulk.
- [2026-05-12][phase 1 retrospective] Plan Part B had wrong API-Football league IDs (claimed Botola=322, real=200; Wydad=967, real=968; Raja=968, real=976; Coupe du Trône=670, real=822). Code now imports canonical IDs from src/lib/constants/canonical-ids.ts. Update Part B of plan to match verified IDs when convenient.
- [2026-05-12][phase 2] Première Ligue (France women's top flight) not surfaced in initial canonical-ID lookup. Discover via API during Phase 2 reference-data seed or in Phase 4 cleanup.
- [2026-05-12][phase 2] WC 2026 Qualification Africa (id=29) shows year=2023 in API. Investigate during seeding: may need different ID for the 2026 qualifier cycle, or API may not yet have populated the 2026 season.
- [2026-05-12][phase 2] Vary upgrade decision: stay on current plan (limit_day=7500) until Phase 2 seeding tells us if 7500 is enough. Upgrade to higher-tier (75k/day) only if real seeding hits the cap.
- [2026-05-12][phase 2.2 retro] drizzle-kit migrate does not auto-load .env.local. Future cron jobs or CI scripts must explicitly `set -a && source .env.local && set +a` before invoking drizzle-kit commands. Document in scripts/README.md or seed.ts header when those land.
- [2026-05-12][phase 2 retro] API-Football upgraded from Pro to Ultra. New daily limit: 75,000 requests/day (verify with /status). Previous concern about 7,500 limit is resolved. Phase 2 player seeding can run in single passes without throttling.
- [2026-05-12][phase 4] At Phase 4 start, read docs/research/atlaskings-v2-navigation-supplement.md and decide which Sofascore conventions to adopt: URL patterns, tab state via hash, slug ordering, breadcrumb structure, SWR refresh intervals, cache TTLs. Document decisions inline in the Phase 4 proposal.
- [2026-05-12][phase 5] Featured-match selection algorithm for homepage carousel. Sofascore uses (homeTeam.userCount + awayTeam.userCount) — we have no equivalent signal from API-Football. Default candidate: tier-based ranking (Tier 1 WC > Tier 2 Morocco > Tier 3 WAFCON > Tier 4 top-5 EU > Tier 5 other), with secondary sort by kickoff proximity (closest upcoming first). Decide at Phase 5 proposal time.
- [2026-05-12][phase 3+] Saudi Premier League Women (id=1227, season 2026 active) discovered during Tier 6 verification. Consider adding to VERIFIED_COMPETITIONS as Tier 5 (women's) addition.
- [2026-05-12][phase 2.4] Algeria Ligue 1 (186) and Tunisia Ligue 1 (202) have statistics_fixtures=false in coverage. Phase 8 UI must check coverage flags before rendering the Statistics tab for these leagues.
- [2026-05-12][phase 11+] Upstash Redis quota tracking deferred. Currently dead code (no-op when unconfigured). v2 scale (~600 calls/day) does not require it. Revisit if API-Football rate limiting becomes a real issue or if quota dashboard is needed for ops.
- [2026-05-12][phase 2.3 retro] Country-name-to-code resolution was missing across ingestion mappers (teams, venues). Caused FK violation on first real seed attempt. Fixed via buildCountryLookup() + resolveCountryCode() helper with alias map for API-Football quirks (England→GB-ENG, South-Korea→KR, etc). Future ingestion mappers MUST use this pattern for any country reference.
- [2026-05-12][phase 12] Optimize workers/live-poller/Dockerfile: add .dockerignore, multi-stage build, only copy what the worker actually needs from src/lib/. Current Dockerfile is functional but bloated for Railway production deployment.
- [2026-05-12][phase 4] shadcn style is "base-nova" (Base UI primitives), not the older "new-york" (Radix) preset referenced in some plan/research docs. Component APIs are similar but imports come from @base-ui/react. Document any deviations as encountered.
- [2026-05-13][phase 5] Live-poller worker must populate homeName/awayName in ScoreUpdatePayload when pushing to Pusher. Currently sends fixtureId + scores only. LiveTicker component falls back to "Team" when names are absent.

- [2026-05-13][phase 4.5] Visual richness pass. Sofascore visual analysis at docs/research/sofascore-analysis-atlas-kings-phase4-5.md is the authoritative reference. Three product decisions to lock before implementation: (a) LeftRail scope at current data state, (b) LiveTicker integration vs row-level live styling, (c) homepage hero philosophy. Target 8-12 writes across 2-3 sub-tasks.

- [2026-05-13][phase 6+] Inner-page 3-column shell — one reusable layout primitive used by competition/team/player/match-detail pages. Measurements at 1280px viewport: page gutter ~16px, left rail ~360-380px, gap ~16px, center column ~500-520px flexible, gap ~16px, right rail ~280-300px, page gutter ~16px (total ~1224px usable). Homepage explicitly does NOT use this shell — stays single-column. Current LeftRail.tsx at 280px is too narrow for what fits inside (featured match card with crests + score + kickoff, fixture rows with team names) — widen to ~360px when reintroduced, or rebuild entirely. Atlas Royal palette applied throughout.

- [2026-05-13][phase 6+] Right-rail editorial content (replaces Sofascore's ad slot since we don't run ads per Loi 09-08). Component library: TopScorersWidget, TrendingPlayersWidget, RecentResultsCarousel, H2HSummaryCard, MoroccoEditorialPicks, MeetTheTeamsCard (tournament pages), MoreMatchesTodayWidget. Each ~300px wide, ~250-400px tall, reusable across page types. This is genuine differentiation territory — Sofascore wastes this space on bet365 banners; we fill with useful content. Particularly valuable for Morocco editorial picks on WC 2026, AFCON, WAFCON team intro cards via API-Football CDN player photos.

- [2026-05-13][phase 6+] TournamentProgressBar component for cup/knockout competitions. Horizontal stage strip showing Group stage → R32 → R16 → QF → SF → 3rd → Final with dots/markers for completed and current stages. Applies to ~12 of 36 verified competitions: WC 2026 (id=1), AFCON (6), WAFCON (922), CAF Champions League (12), CAF Confed (20), Coupe du Trône (822), UCL (2), UEL (3), UECL (848), UWCL (525), UEFA Europa Cup Women (1191), AFCON Qualifiers (36). Driven by VERIFIED_COMPETITIONS metadata — add hasKnockout boolean flag per competition. Atlas Kings brand opportunity: use Morocco flag colors (red → gold → green) for the gradient where Sofascore uses generic red-orange-gold-green. Genuinely better visual story than Sofascore's blue-only palette.

- [2026-05-13][phase 6+] EditionSelector component for tournaments with multi-edition history. Horizontal carousel of past tournament years (2026 / 2022 / 2018 / 2014 / 2010...). Each edition links to a separate competition page with that year's data. Applies to WC, AFCON, WAFCON, UCL, UEL, UECL, Olympics Football (when added). Default selection is current edition.

- [2026-05-13][phase 6+] MatchesViewSwitcher segmented control with three modes: By date (chronological), By round (grouped by Round 1, 2, 3...), By group (grouped by Group A, B, C — tournament-only, hidden for leagues). Lives in left rail's Matches section on competition pages. Default By date for currently-active tournaments, By round for completed/upcoming league seasons.

- [2026-05-13][phase 6+] Trending page (/[locale]/trending) — master-detail UI distinct from standard inner-page shell. Left rail wider (~480px, not 360px) to fit fixture rows with full event metadata (sport, competition, country, team names, scores, kickoff times). Center shows selected event's detail card with own tabs (Details/Box score/Statistics/Commentary/Games/Media). Right rail editorial content. Clicking left items updates center without full page navigation (Next.js parallel routes or client-side state). Atlas Kings curation favors Morocco-relevant events: Botola Pro live matches, Atlas Lions fixtures, Wydad/Raja/FAR results, Hakimi/Ziyech/Mazraoui appearances.

- [2026-05-13][phase 6+] Layout matrix by page type (canonical reference for inner-page rendering):
  * Homepage: single column, no rails
  * Competition (League) e.g., Botola Pro, Premier League: left rail (Featured match card + Matches list with round selector) + center (Standings table with All/Home/Away tabs, then Stats/Details/Media tabs) + right rail (editorial)
  * Competition (Cup/Tournament) e.g., WC 2026, Coupe du Trône, AFCON: same as league + TournamentProgressBar above tabs + EditionSelector + MatchesViewSwitcher in left rail
  * Team: left rail (Featured match + Recent form) + center (Details/Matches/Standings/Squad/Top players/Statistics tabs) + right rail (top players, trending)
  * Player: left rail (career summary + current club) + center (Details/Season/Matches/Career tabs) + right rail (similar players, news)
  * Match Detail: left rail (lineups for one team) + center (Details/Odds-replaced/Lineups/Matches/Standings-or-Knockout/Media tabs) + right rail (H2H summary, predictions, more matches today)

- [2026-05-13][phase 4.5+ typography] Switch Latin face from Inter to IBM Plex Sans for deliberate pairing with IBM Plex Sans Arabic (same designer, same studio, intentional system — Sofascore can't match this because they don't prioritize Arabic). Add Recoleta or Cormorant as display face for editorial moments (page titles on team/competition pages, match detail headers, hero text). Recoleta is paid via Adobe — verify Adobe Fonts subscription or use free alternative (Cormorant from Google Fonts, Newsreader display variant, or Fraunces). Apply Level 1 patterns rigorously: tabular numerals via font-variant-numeric: tabular-nums on all scores/times/ages, tight line-height 1.2-1.3 on data-dense components, weight scale 400 body / 500 labels / 600 active+headings / 700+ display only, smaller body size 13-14px for data-dense UI vs 16px default. Implementation: Phase 4.5b (after current Phase 4.5a chrome polish completes) — update next/font/google imports in src/app/[locale]/layout.tsx, update --font-* CSS variables in tokens.css, retrofit existing components to use semantic weight scale.

- [2026-05-13][phase 4.5+ typography DECISION LOCKED] Display face = Fraunces (Google Fonts, variable, free). Pairs with IBM Plex Sans for editorial display moments: page titles on team/competition/player pages, match detail headers, hero text. NOT used for body/UI labels/data display. Body = IBM Plex Sans Latin (pairs with IBM Plex Sans Arabic). Weight scale: Fraunces 400-700 display, IBM Plex Sans 400/500/600/700 semantic (body/labels/active+headings/strong-emphasis). Variable axes: weight + opsz + soft. Implementation in Phase 4.5b after current chrome polish 4.5a ships.
- [2026-05-14][phase 5] team.md Stats tab (Block 1 — Attaque) includes "Grosses occasions par match" (big chances per match — aggregate). Verify during Phase 5 data ingestion that API-Football /teams/statistics actually returns this field for covered leagues. If absent or unreliable, drop from Block 1 before Phase 8 team-page Stats tab UI build. Distinct from per-shot big-chance attribution (Sofascore proprietary) which is already cut. Surfaced by 2026-05-14 schematic audit pass over competition-cup.md, team.md, player.md, match.md — that audit otherwise found zero false-positive specs of unavailable features.
- [2026-05-14][phase 5+] Page 2 (competition-league.md) Standings Tracker chart retrofitted out of v1 scope on 2026-05-14. API-Football /standings endpoint provides only current-snapshot standings — no per-round historical archive — so a Tracker visualization is not buildable from API data alone. Slot in competition-league.md now marked "reserved, replacement TBD post-Phase-5 data ingestion." Revisit post-Phase-5 once we have a populated fixtures table: per-round standings can be reconstructed by replaying finished fixtures in date order, which would unlock a Tracker rebuild as a derived dataset. Decision and replacement-feature design owed at that point. Commit: docs(schematic): retrofit Page 2 Standings Tracker as deferred slot.
- [2026-05-14][phase 6+] [SUPERSEDES 2026-05-13 layout matrix entry] The 2026-05-13 "Layout matrix by page type (canonical reference for inner-page rendering)" entry above is superseded as of 2026-05-14. Each page's layout is now canonically specced in its own schematic document under docs/schematics/ (homepage.md, competition-league.md, competition-cup.md, team.md, player.md, match.md). The matrix bullet list in the older entry is retained for history but is no longer the source of truth. Note: other 2026-05-13 [phase 6+] entries (inner-page 3-column shell, right-rail editorial components, TournamentProgressBar, EditionSelector, MatchesViewSwitcher) remain canonical — schematics reference them as the source spec.
- [2026-05-14][phase 6] Plan deviation: Phase 6 (Competition page) reordered before Phase 5 (Homepage). Rationale: homepage is a structural singleton (single column, no inner-page shell) and cannot serve as the canonical visual reference for the rest of the site. competition-cup.md (WC 2026 instance) selected as the first build target because (a) it is the superset of competition-league.md (inherits the 3-column inner-page shell + adds cup-specific components: TournamentProgressBar, EditionSelector, MatchesViewSwitcher, knockout bracket), (b) WC 2026 is the launch-day content priority per Part N of the plan, (c) WC 2026 data is fully seeded per Phase 2 exit criteria (104 fixtures, 48 teams, 12 groups). Architectural rule for this build: every cup-specific UI component must be self-contained so competition-league.md can instantiate the same shell without rendering them. Known constraint: WC 2026 is pre-tournament (kicks off 11 June 2026), so visual review validates the pre-tournament/scheduled state only — populated/live/post-tournament states reviewable after matches play. Phase 5 (Homepage) reverts to canonical order after Phase 6 ships. Phase 4.5a chrome work (SportNav + Footer + i18n) retained on disk uncommitted; will be absorbed and revised inside the Phase 6 WC 2026 build rather than reverted or committed separately.
- [2026-05-14][phase 6+] getFeaturedMatch in queries.ts takes moroccoTeamId as a required param, coupling it to a Morocco-first selection strategy. When competition-league.md is instantiated for Botola Pro, the featured match algorithm needs different logic (Wydad/Raja next match, or stakes-weighted selection). Generalize into a strategy pattern or discriminated config param in Phase 6 league sub-task.
- [2026-05-14][phase 6 sub-task 6.1 retro] Three corrections owed from Sub-task 6.1 implementation: (a) BACKLOG L54 typography decision wording says "page titles" for Fraunces display face — this means About-card section titles, NOT H1 (H1 uses IBM Plex Sans per competition-cup.md Section 2). Update L54 wording when convenient. (b) SportNav.tsx was deleted outright in Sub-task 6.1 rather than "absorbed and revised" as the Phase 6 plan-deviation entry (L63) stated — the single-sport nav concept was folded into the Topbar, not preserved as a separate component. (c) Typography swap (Inter to IBM Plex Sans + Fraunces) shipped inside Phase 6 Sub-task 6.1 rather than as a standalone Phase 4.5b task — the swap was a prerequisite for competition page chrome and could not be deferred further.
- [2026-05-14][phase 6 sub-task 6.5] Rewrite AdaptiveTopStrip from static "WC 2026 group preview + countdown" to a dynamic live/upcoming match ticker. Driven by DB, not static config or editorial curation. Behavior: query fixtures across ALL tracked competitions; if any matches are status=live now, show those; if zero live, show the next 15 upcoming fixtures by kickoff time ASC. Horizontal scroll handles overflow. Replace current WC 2026 countdown (drop entirely — countdown was useful pre-tournament but doesn't generalize). Same component renders on every page, no context-awareness. Scope of competitions = all 36 verified competitions seeded per Phase 2. Sub-task 6.5 follows 6.4 (right rail) in Phase 6 sequence — must ship before Phase 5 (homepage rebuild) because homepage depends on the AdaptiveTopStrip. Schematic deviation from homepage.md §1 (which speccs 4 modes including the countdown). Document deviation in the eventual implementation BACKLOG retro.
- [2026-05-14][phase 6 sub-task 6.3] Best 3rd-placed teams tab: FIFA full tiebreaker chain requires two unseeded data sources. (a) Fair-play tally (yellow + red cards per team aggregated from fixture events) — derivable post-Phase 5 ingestion once events data is populated. (b) FIFA Men's World Ranking — no API-Football endpoint exists; requires external data source, manual config table, or Phase 12+ admin tooling. Both are also needed for FifaRankingBadge (stubbed with null). Current Best 3rd sort falls back to stable order when tied through goalsFor; ⓘ disclaimer tooltip surfaces this to the user.
- [2026-05-14][phase 6 sub-task 6.3] CenterTabs hashchange listener missing: MiniStandingsPreview dispatches HashChangeEvent via window.dispatchEvent(new HashChangeEvent('hashchange')) but CenterTabs only reads hash on mount (useEffect with [tabs] dep). The "View all groups" button sets the hash but doesn't trigger tab switch within SPA navigation. Fix: add a hashchange event listener in CenterTabs useEffect. Low priority — only affects in-page cross-tab navigation from Overview → Standings.
- [2026-05-14][phase 6 sub-task 6.3 retro] Sub-task 6.3 (Center column tabs) implementation notes and deviations: (a) Tab content H2 ("Overview — World Cup 2026") removed — redundant with page H1 + active tab label. Schematic implied per-tab section heading; deviation acceptable. (b) FeaturedMatchCard footer simplified — removed "Group Stage - 1" (redundant with page context) and "FIFA: — vs FIFA: —" (rank data not seeded). Added stadium/city line from hydrated venue field. (c) Knockout bracket rendered as vertical list per phase, not converging CSS bracket. Visual bracket deferred — pre-tournament data is identical placeholders across all slots, list communicates as clearly. Build proper bracket when real knockout data exists OR as discrete v1+ polish task. (d) MiniStandingsPreview groupLabel normalization at query layer ("Group A" to "A") to match metadata labels. Pattern to follow for future joins. (e) HistoricalWinners ordinal handling via formatOrdinal() helper — produces 1st/2nd/3rd (EN), 1er/2e/3e (FR), plain numbers (AR). Use this helper for any future ordinal-in-text rendering. (f) Next.js 16 dynamic route params asymmetry: page components receive percent-encoded values, generateMetadata receives decoded values. Non-ASCII slugs (Arabic) fail comparison in page components without explicit decodeURIComponent. Fix applied to CompetitionPage + 3 placeholder routes (equipe, joueur, match). Pattern for any future dynamic route with non-ASCII segments: decodeURIComponent at the top of the page component before any string comparison or display. (g) Best 3rd-Placed Teams tab added as a 4th tab between Standings and Knockout, gated by metadata.hasBestThirdPlace flag in CupMetadata. FIFA tiebreaker chain partially implemented (points DESC, goalDiff DESC, goalsFor DESC). Fair-play tally and FIFA World Ranking tiebreakers fall through to stable sort — fair-play data needs Phase 5+ ingestion derivation from event data, FIFA Ranking needs external data source (no API-Football endpoint, Phase 12+ admin tooling or manual config table). Conditional disclaimer renders only when ties beyond GF actually exist in visible data. (h) Arabic team names not populated in team.name['ar'] jsonb for most teams — falls through to English. Same data issue as Scotland from 6.2 retro. Phase 5+ data ingestion should backfill name['ar'] across all 48 WC 2026 teams and all teams used in MoroccoContextLine / FixtureRow / standings tables. Tracked here so it is not lost.
- [2026-05-15][phase 6 sub-task 6.4 retro] Sub-task 6.4 (Right rail + Videos + About + FAQ + SEO + Share primitives) implementation: (a) Right rail widgets shipped: TopScorers, TopAssists, MeetTheTeams (Group C teams), MoreMatchesToday, NewsletterCard (reused from left rail). TopScorers/TopAssists/MoreMatchesToday render compact empty state pre-tournament — single line of explanatory text, no share button, share appears automatically when data populates. (b) Videos section as full-width section below the 3-column grid. Empty state until YouTube IDs are curated in Phase 9. (c) About card with 6 H2 sections + FAQ (collapsible details). Content sourced from competition-cup.md §11 — EN/AR drafted from FR with REVIEW comments. User polish post-commit. (d) SportsEventJsonLd component renders application/ld+json structured data for SEO. Per-fixture SportsEvent deferred to Phase 8. (e) Standings refactor: StandingsTab and MiniStandingsPreview changed from single container card holding 12 stacked tables to 12 separate cards per group, each with id="group-x" and its own share button. Schematic deviation; enables per-card sharing per the Phase 7+ shareable-cards-architecture initiative. (f) ShareButton (src/components/shared/ShareButton.tsx) and HashScrollHighlight (src/components/shared/HashScrollHighlight.tsx) generic primitives shipped. Used by WC 2026 now, reusable for any tournament/league/team/player/match page going forward. (g) Hash-fragment deep linking with Latin keys regardless of locale (e.g. #group-c, #meet-teams). Cross-locale share compatibility — a link shared from /fr lands correctly on /ar. (h) ShareButton hides when card is in empty-state (TopScorers, TopAssists, MoreMatchesToday, Videos). Reappears when data populates. (i) MeetTheTeams renders flag + team name only — captain photos deferred until squad data is hydrated in Phase 9+. (j) Sub-task 6.4 ships the v1 of the shareable-cards-architecture initiative (hash-fragment deep links). Full per-card URL refactor with OG images and per-card JSON-LD follows in Phase 7+.
- [2026-05-15][phase 6] tournament-metadata.ts group compositions (L78-90) do NOT match seeded DB standings data. Hardcoded groups were speculative pre-draw data; DB standings from API-Football are the ground truth. Example: metadata Group C = ['AR', 'MA', 'SA', 'EG'] (Argentina, Morocco, Saudi Arabia, Egypt) but DB Group C = Brazil, Morocco, Haiti, Scotland. ALL 12 groups have similar discrepancies. hostCountryCodes fixed (removed MA), but groups array needs full reconciliation against DB. Groups array is not actively rendered on the page (GroupPills removed in 6.5 ticker rewrite, MeetTheTeamsCard uses DB standings), but it is a latent source of factual errors if any future component reads from it. Fix before any component re-reads metadata groups.
- [2026-05-15][phase 6] Factual error audit (2026-05-15): Morocco was incorrectly listed as a WC 2026 co-host across page.tsx (intro, facts, metadata descriptions), about-content.ts (3 locales, 6 sections + 8 FAQs each), and tournament-metadata.ts (hostCountryCodes). All corrected. WC 2026 hosts are USA, Canada, Mexico only. Morocco co-hosts WC 2030 with Spain and Portugal. Group C corrected from Argentina/Saudi Arabia/Egypt to Brazil/Haiti/Scotland per DB standings.
- [2026-05-15][phase 7+ design] Curated SVG competition shield icon set. Current ticker uses raster competition.logoUrl from API-Football (varying quality, some non-square, no dark-mode support). Replace with a curated SVG icon library mapped by competitionId. ~36 SVGs needed for tracked competitions. Each SVG should be: square aspect ratio, currentColor-friendly for theme switching, ~24x24 base size scaling cleanly to ticker (20px), card headers (32px), hero (48px). Hosted as a single sprite or per-icon. Mapping in src/lib/icons/competition-shields.tsx. Falls back to raster logoUrl when no SVG is available for a competition. Worth 1-2 days of design work. Defer until visual polish phase after Phase 6 completes.
- [2026-05-15][phase 5+] Two stale fixtures in DB with status_code='2H' but team references resolve to incomplete data (JS Saoura id=914 has code=null). Rendered as "???" cells in ticker before render-time filter was applied. Fixture IDs: 1391175 (Osasuna vs Atletico Madrid, La Liga), 1513400 (JS Saoura vs CR Belouizdad, Algeria Ligue 1). Phase 5 ingestion should either backfill these to finished status (FT) or ensure all team records have a non-null code.
- [2026-05-14][phase 6 sub-task 6.6] Knockout bracket visualization. Current Sub-task 6.3 implementation renders knockout phases as a vertical list of match cells per phase (R32 / R16 / QF / SF / 3rd / Final), accessed via phase selector chips. This communicates structure but not progression — fans cannot see which winner of Match X plays the winner of Match Y in the next round. Build a proper converging tournament bracket. Desktop: classic symmetric layout — left side renders 8 Round of 16 matches converging rightward, right side renders 8 Round of 16 matches converging leftward, Quarter-finals on both inner sides, Semi-finals, then Final at center. Round of 32 on far outer columns. Connecting lines drawn between adjacent matches and their successor cell. Mobile (~380px viewport): full bracket does not fit. Render as horizontal swipe between rounds — R32 first card, swipe to R16, swipe to QF, swipe to SF, swipe to Final. Each round shows its matches stacked vertically. No connecting lines on mobile (clutter). Tablet (~768px): degraded desktop bracket with smaller text and tighter spacing, or fall through to mobile pattern at threshold. RTL: full bracket mirrors — left-side rounds become right-side rounds in Arabic. Pre-tournament state: bracket renders with placeholder labels ("1st Group A" / "2nd Group B" per FIFA seeding rules) and connecting lines draw between empty cells to show the structural flow. Once group stage plays out, real team names populate. Replace the current PhaseSelector + KnockoutMatchCell list pattern with a new KnockoutBracket component. Keep PhaseSelector as a fallback for mobile (alternate to swipe if needed). Sub-task 6.6 follows 6.5 (AdaptiveTopStrip ticker) in the Phase 6 sequence — completes the WC 2026 competition page polish before Phase 5 homepage rebuild.
- [2026-05-15][phase 6 sub-task 6.7+ content brief] WC 2026 competition page content enrichment + SEO scaffolding. Two related but separable work blocks. Block A — content + structure (this entry); Block B — SEO scaffolding (follows Block A). NO word-count padding — write only what serves the user, accept whatever length results. Drafting flow: Claude Code drafts EN first against this brief with required structural classification (prose/table/list/stat-card/timeline/FAQ) for every fact. User reviews EN for factual accuracy and structure fit. Then FR and AR draft from approved EN with REVIEW comments for user verification. STRUCTURAL PRINCIPLE: never one giant "About" wall of text (Sofascore pattern, avoid). Content is split into thematic cards, each independently rendered with content-type-appropriate UI. Internal card IDs (used for hash anchors and component refs) stay stable; user-facing H2 text follows user-intent naming per locale. Required content types and rendering rules: Type 1 PROSE — narrative paragraphs (max 3-4 sentences each), used for storylines, history, qualitative analysis. Type 2 TABLES — structured data with semantic <table> markup, used for venues, tiebreakers, broadcasters, contenders. Mobile responsive (horizontal scroll or stacked card layout). Type 3 STAT CARDS — single-fact quick facts in a grid, used for top-of-page quick-facts strip. Type 4 TIMELINE — chronological information, used for Morocco fixtures and key dates. Type 5 LISTS — semantic <ul>/<ol> with structure, used for debutants, returnees, tips. Type 6 FAQ — <details>/<summary> accordions with FAQPage JSON-LD wrapper. CARD INVENTORY (internal ID → user-facing H2 EN / FR / AR): (1) QuickFactsStrip — no header, visual anchor. Grid of 6 stat cards: 48 teams, 12 groups, 104 matches, 16 venues, "June 11–July 19 2026", 39 days. (2) about-format → "The 48-team format" / "Le format à 48 équipes" / "نظام البطولة بـ 48 منتخباً". 2 prose paragraphs + tiebreaker order TABLE + "Compared to 2022" callout. (3) about-venues → "Host cities and stadiums" / "Villes hôtes et stades" / "المدن المضيفة والملاعب". 1 prose intro + TABLE of 16 venues × {city, country, stadium, capacity, matches hosted} + "Travel implications" callout. (4) about-morocco → "Morocco at the World Cup" / "Le Maroc à la Coupe du Monde" / "المغرب في كأس العالم". 2-3 prose paragraphs (2022 semi-final context, current squad Hakimi/En-Nesyri, Group C analysis) + Morocco fixtures TIMELINE (3 group games with date/venue/opponent, links to match pages — verify against seeded fixture data) + diaspora viewing sub-paragraph. (5) about-teams → "Qualified teams" / "Équipes qualifiées" / "المنتخبات المتأهلة". 1 paragraph continental balance + LIST debutants (Cape Verde, Curaçao, Jordan, Uzbekistan) + LIST returnees (Scotland 28y, Iraq 40+y) + "See all teams" link. (6) about-storylines → "Storylines to watch" / "Récits à suivre" / "أبرز القصص". Prose paragraphs (Messi/Ronaldo era, defending champion Argentina, expanded format implications) + 4-5 bulleted sub-points. (7) about-tickets → "Tickets and hospitality" / "Billets et hospitalité" / "التذاكر والضيافة". Prose intro + BULLET LIST ticket categories with price ranges (~$60 group to $thousands final, On Location/Qatar Airways hospitality) + scam-awareness WARNING callout + FIFA.com/tickets link. (8) about-watch → "Live on TV and streaming" / "En direct à la TV et en streaming" / "مباشر على التلفزيون والبث". Prose intro + TABLE broadcasters by region (Morocco/Europe/Americas/Asia/Africa with FIFA+ global option) + Morocco callout (Arryadia/SNRT, beIN, diaspora viewing tips, time zones). (9) about-travel → "Travel, visas, and fan zones" / "Voyager, visas et fan zones" / "السفر والتأشيرات ومناطق المشجعين". Prose intro on 3000-mile spread + VISA REQUIREMENTS table by passport country + BULLET LIST budget vs luxury examples + sustainability sub-paragraph + US-Mexico border logistics callout. (10) about-predictions → "Predictions and favorites" / "Pronostics et favoris" / "التوقعات والمرشحون للقب". Prose context + TABLE pre-tournament contenders with odds (Spain, France, England, Argentina, Morocco as African hope) + BULLET LIST dark horses + Golden Boot candidates + Official FIFA fantasy link. (11) about-legacy → "Tournament legacy and 2030 outlook" / "Héritage du tournoi et perspectives 2030" / "إرث البطولة وآفاق 2030". Prose paragraphs economic/cultural/legacy impact (tourism, infrastructure, jobs, cultural exchange) + balanced view (local resident burden, climate concerns, immigration policy debates) + tie to WC 2030 (Spain/Portugal/Morocco + South American opening matches). (12) FAQ → "Frequently asked questions" / "Questions fréquentes" / "الأسئلة الشائعة". 12-15 Q&As as <details>/<summary> accordions, each with own ID for deep linking, wrapped in FAQPage JSON-LD. Suggested Qs: how does 48-team format work, when does Morocco play, where to watch in [country], tickets still available, visa requirements, will Morocco's group be tough, tiebreaker rules, why three host nations, what stadium hosts the final, accessibility for disabled fans, family travel options, last-minute schedule changes. EDGE CASES to address explicitly: accessibility (disabled fans seating/transport), family travel logistics, schedule-change disclaimer pointing to FIFA.com for latest official info. IMPLICATIONS angle: 48-team format means more global representation but risks of diluted quality; co-hosting model may set precedent for future events like WC 2030 — discuss fairly with both pros and cons. NEW REUSABLE COMPONENTS to build (each lives in src/components/competition/about/ or similar): QuickFactsStrip (stat-card grid, props for any competition's quick facts), VenueTable, MoroccoFixturesTimeline (rename generically to TeamFixturesTimeline for reuse), BroadcasterTable, VisaRequirementsTable, ContendersTable, FaqAccordion (wraps <details>/<summary> with FAQPage JSON-LD). ~7 new components. Each generic enough to work for Botola Pro and other competition pages with different data. Block B — SEO scaffolding (separate sub-task, follows Block A): richer SportsEvent JSON-LD (organizer FIFA, subEvent array per match, competitor array per team, superEvent FIFA series, awards, previousWorldChampion). FAQPage JSON-LD wrapping the FAQ section (above). BreadcrumbList JSON-LD for the nav breadcrumbs. Article JSON-LD for the content body with datePublished/dateModified. Complete Open Graph meta (og:title/description/image/url/type=website/locale). Complete Twitter Card (summary_large_image with og:image source). hreflang link tags for EN/FR/AR variants. Canonical URL link tag. Robots meta. Heading hierarchy audit (H1 → H2 → H3 clean outline across the entire page including tabs). Image alt-text audit across competition logos, team flags, stadium photos, all images. Anchor text audit on every internal link (full readable text, never "click here"). Sequence: Block A drafted in EN → user review → FR draft + AR draft → user review per locale → Block B scaffolding once content stabilizes. Block A pattern folds into competition-league.md instantiation later — same 12-card structure applies to Botola Pro with content swapped per competition. Block B scaffolding lives in shared components used across all competition pages.
- [2026-05-15][phase 6 sub-task 6.6 sidenote] Pre-existing tsc error in tests/unit/workers/diff.test.ts line 28: "'id' is specified more than once" — explicit id key is overridden by ...overrides spread. Not blocking Phase 6 work but worth a one-line cleanup in a future pass.
- [2026-05-15][phase 6 sub-task 6.6+] tournament-metadata.ts group compositions confirmed wrong via PDF extraction. PDF Group C = BRA, MAR, HAI, SCO. Metadata Group C shows different teams. All 12 groups need reconciliation. Phase 6.6+ follow-up: rewrite tournament-metadata.ts groups array using WC_2026_SCHEDULE as source of truth. Until reconciled, do NOT render anything from metadata.groups (DB standings already used for visible UI, so impact is contained).
- [2026-05-18][phase 7 audit — RESOLVED] Timezone handling: no bug. DB fixtures store UTC correctly (verified: Match 1 MEX vs RSA = 2026-06-11T19:00:00.000Z = 15:00 EDT). API-Football sends ISO 8601 with UTC offset, ingestion via new Date(f.date) preserves it, Postgres timestamptz stores UTC. All 6 display components use Intl.DateTimeFormat without explicit timeZone param → browser converts UTC to user-local time automatically. WC_2026_SCHEDULE.kickoffEt (ET text) is never rendered — bracket cells only use formatShortDate (date, not time). Original concern (ET stored raw, display shows wrong timezone) was based on a wrong premise. Supersedes 2026-05-15 entry.
- [2026-05-15][phase 6 sub-task 6.6 sidenote] Earlier R32 venue data provided in chat by Claude (chat assistant, not Claude Code) had ~10 errors (M76, M82, M84, M85, and 6 others incorrect). PDF parsing via pdftotext is the authoritative extraction. Any earlier wc2026-bracket.ts proposal draft that used hand-extracted venue data is superseded by wc2026-schedule.ts. When the new Chunk 4 (bracket integration) uses this data, all venues must come from WC_2026_SCHEDULE, not any earlier hand-typed list.
- [2026-05-16][phase 6 sub-task 6.6] BracketPageClient and KnockoutTab share near-identical logic (PhaseSelector + KnockoutBracket + WC_2026_BRACKETS_BY_LOCALE lookup). Kept separate intentionally — dedicated bracket page will diverge (zoom controls, legend, print view). If the two stay tightly aligned through Phase 7, extract a shared useKnockoutBracketData hook then.
- [2026-05-16][phase 6 sub-task 6.6] BUG: BracketConnectors draws a line directly between SF-1 (M101, col 4) and SF-2 (M102, col 6) as if they feed each other. Both SFs have feedsInto='m104' (Final). The connector logic groups sources by feedsInto target — finding two sources for 'm104', it draws an L-shaped connector between them as a "pair". This works for same-column pairs (R32→R16, R16→QF, QF→SF) but breaks for SF→Final because the two sources are on opposite sides of the bracket (columns 4 and 6). Fix: BracketConnectors should detect when two source cells are in different columns separated by ≥2 grid columns and draw two independent horizontal lines to the target instead of one merged L-connector. Alternatively, skip connector drawing when sources are on opposite sides.
- [2026-05-16][phase 6 sub-task 6.6] Visual: Final and 3rd place cells in center column (col 5) have too much vertical gap between them. gridRow '4 / span 5' and '10 / span 5' leaves rows 9 empty. Tighten on the dedicated bracket page where full viewport width gives more control over row sizing.
- [2026-05-16][phase 7+] Cape Verde team name localization: API-Football stores "Cape Verde Islands" as the English team name. FR should be "Cap-Vert", AR should be "الرأس الأخضر". Requires data migration or ingestion post-processing to set localized names for teams whose API-Football name differs from the standard localized form.
- [2026-05-16][phase 8] WC 2026 bracket team resolution: once live data ingestion populates knockout fixtures (Phase 7 poller), bracket cells should resolve actual team names/codes from DB instead of showing FIFA slot codes (W74, 1A, etc.). Requires a buildLiveBracket() variant that queries fixtures table and falls back to slot codes for unresolved matches.
- [2026-05-17][phase 7+] Related Competitions section shows only 1 entry (AFCON Qualifiers id=29). The other 6 relatedCompetitionIds (32, 31, 34, 30, 33, 37 — WC qualification per confederation) are not in MEGA_MENU_SECTIONS so RelatedCompetitions.tsx can't resolve them. Fix: add WC qualification competitions to competitions-mega-menu.ts, or change RelatedCompetitions to resolve from a broader source (e.g. canonical-ids or DB). Scope: site-wide nav impact, defer to Phase 7+.
- [2026-05-17][phase 7+] Ticker pause button for a11y. TickerStrip marquee auto-scrolls via CSS animation. WCAG 2.2.2 requires a mechanism to pause moving content. Add a pause/play toggle button (visible on hover/focus, always visible to screen readers). Deferred: requires client state + coordination with Phase 7 Pusher live ticker channel.
- [2026-05-17][phase 11] Create proper 1200x630 OG image for WC 2026 competition page before launch (Phase 11 or earlier). Current OG uses trophy PNG (public/competitions/wc-2026-trophy.png) which is wrong aspect ratio. Phase 11 adds Vercel OG dynamic image generation for all competition pages.
- [2026-05-17][phase 7+] TickerStrip.tsx teamLabel() uses code-first fallback (code → shortName.en → name.en truncated). Not locale-aware — renders English codes on AR/FR. Phase 7+ Pusher ticker rewrite should adopt getTeamDisplayName or a locale-aware compact variant.
- [2026-05-18][phase 7+] EditorialHero modes A/B show competition name via inline fallback (competitionName[locale] || competitionName.en). Should use getLocalizedCompetitionName but the query doesn't select competitionId. Fix: add compId to editorial-hero.ts queries + HeroRow/HeroFixture types.
- [2026-05-18][phase 7+] Ingestion shortName default fixed (7.9) but ~240 existing club teams in DB still have shortName.en = 3-letter code (the old default). Need a backfill script (same dry-run + --apply pattern as scripts/backfill-team-names.ts) to SET shortName.en = name.en WHERE shortName.en = code for club teams. Defer to separate sub-task.
- [2026-05-17][phase 7+] Tournament components (GroupTable, FeaturedMatchCard, BestThirdTable, MiniStandingsPreview, MeetTheTeamsCard, MoroccoContextLine, FixtureRow) have inline name[locale] ?? name['en'] fallback chains. Could be consolidated to use shared getTeamDisplayName utility. Low priority — they already use the correct order (name before shortName).
- [2026-05-18][phase 7] TickerStrip TeamCell uses raw <img> for club logos (line ~49). Should use next/image for optimization (lazy loading, srcset, blur placeholder). Low priority — logos are 16×16 and already lazy-loaded via browser native, but next/image would add Vercel image CDN benefits.
- [2026-05-18][phase 9] Team and player pages are still placeholders. Wire Media tab (query by teamId / playerId) when those pages get built (Phase 10+ or later).
- [2026-05-18][phase 9] VideosSection.tsx is now unused (replaced by CompetitionMediaSection). Safe to delete in a cleanup pass.
- [2026-05-18][phase 9] tournament-metadata.ts still has mediaYoutubeIds field (always empty array). Can be removed now that CompetitionMediaSection queries DB directly. Low priority — field is unused.
- [2026-05-18][phase 9] Admin UI AddVideoForm.tsx (~line 100) and VideoTable.tsx (~line 69) use raw <img> for YouTube thumbnails. Admin-only, external thumbnails with unknown dimensions — next/image needs configuration for i.ytimg.com remote pattern. Low priority since admin UI isn't user-facing performance.
- [2026-05-18][audit] Ticker shortName overflow: getCompactTeamLabel truncates name but not shortName. API-Football shortName for Algerian league teams (e.g. "Olympique Akbou", "El Bayadh", "Khenchela") is the full name, bypassing maxLen truncation. Fix: apply maxLen truncation to shortName fallbacks in getCompactTeamLabel(), not just name.
- [2026-05-18][phase 6] U-17 World Cup 2026: not in API-Football DB at all (no competition entry). Cannot build page until data is seeded. Revisit when API-Football publishes it.
- [2026-05-18][phase 6] WAFCON (id=922) has 0 standings rows in DB. Group tables not seeded by API-Football. Group stage results exist in fixtures (18 group matches, all FT). Could reconstruct standings from fixture results if needed, or wait for API-Football to populate.
- [2026-05-18][phase 6] WAFCON season labeled 2024 in API-Football but fixtures dated July 2025. editionYear=2024 in tournament-metadata.ts matches DB. If API-Football publishes a 2026 season, update editionYear accordingly.
- [2026-05-20][cross-cutting] Season/year filter for all competition pages. Every league and cup page needs a season selector dropdown (driven by `seasons` table) so users can browse historical data. URL-driven (?season=2025 or path segment), default to is_current=true. All data (standings, fixtures, stats, top scorers) scoped to selected season. Applies to both league template and cup template. Examples: PL 2025/26 vs 2024/25; WC 2026 vs WC 2030. Requires: (a) season selector UI component, (b) URL param plumbing in competition page, (c) all queries accept season param, (d) seasons table already has the data.
- [2026-05-20][data correction] WAFCON labeling is wrong. DB has the 2024 edition (played July 2025, all 26 matches FT). Mega menu and cup content label it "WAFCON 2026" but real WAFCON 2026 starts 25 Jul 2026 per verified spreadsheet. Options: (a) relabel current data as "WAFCON 2024" completed tournament, (b) keep as previous edition and add pre-tournament state for real 2026, (c) hide until 2026 data appears. User decision needed.
- [2026-05-20][data correction] Coupe du Trône top scorers data is from season 2024, but current season is 2025.
- [2026-05-20][phase 6+] 15 cups show "coming soon" despite having full data in DB (UCL, UEL, UECL, AFCON, CAF CL, CAF Confed, Coupe du Trône, 7 WC qualifier streams, AFCON Qualifiers). All lack a tournament-metadata.ts entry. Need a generic cup rendering path (standings + fixtures + stats) that works without hardcoded group/bracket metadata.
- [2026-05-20][phase 6+] 10 competitions have data in DB but no mega menu routing: FA WSL, Frauen Bundesliga, Serie A Women, Liga F, UWCL, UEFA Women's Europa Cup, Algeria Ligue 1, Tunisia Ligue 1, Süper Lig, UAE Pro League. Adding mega menu entries would make them accessible.
- [2026-05-24][phase 9] coaches table is empty (0 rows). fixture_lineups has coach_id references (e.g. 2006=Unai Emery, 18=Arne Slot) but coaches table was never populated during ingestion. Need a backfill script to insert coach records from API-Football /coachs endpoint for all referenced coach_ids. Until fixed, the manager names row under the lineup pitch will not render.
- [2026-05-28][data health] 2,473 finished fixtures without player stats — mostly older/minor competition fixtures where API-Football doesn't provide per-player data. Not backfillable from upstream.
- [2026-05-28][data health] 267 finished fixtures without events, 499 without lineups — API coverage gaps for smaller competitions. Not backfillable.
- [2026-05-28][data health] 20 orphan player_ids in fixture_player_stats + 9 orphan assist_player_ids in fixture_events remain after backfill. Guarded by null-safe rendering. Too few to warrant further action.
- [2026-05-29][phase 8+] Match page pre-match data gaps: (a) H2H history missing — fixtures table only has current-season data, no cross-season historical meetings. Need to call API-Football `/fixtures/headtohead?h2h={homeId}-{awayId}` endpoint and ingest results. UCL Final (PSG vs Arsenal) shows "No previous meetings" despite real history. (b) Form card shows "Available closer to kick-off" but recent results exist in DB for both teams — likely a code issue in the match page component, not a data gap. (c) Pre-match enrichment opportunity: "Path to the Final" (each team's knockout journey), season stats comparison, venue info. All derivable from existing DB data without new API calls.
- [2026-05-29][phase 7] HomeUpcomingWeek component (src/components/homepage/HomeUpcomingWeek.tsx) is now unused — superseded by HomeTopMatches. Safe to delete in cleanup pass.
- [2026-06-01][phase 7] Full cacheComponents migration — 4 pages have revalidate route segment configs (competition page, homepage, match page, team page) incompatible with PPR via cacheComponents. Requires replacing revalidate exports with 'use cache' + cacheLife() directives across data-fetching functions. Estimated effort: full audit + migration of 4 page trees. Blocker on competition route ISR caching via PPR. Ref: commit 29d970d added experimental.ppr which is a hard error in Next.js 16.2.6; reverted in follow-up.
- [2026-05-15][phase 6 sub-task 6.5 retro] Sub-task 6.5 (AdaptiveTopStrip ticker rewrite) implementation: (a) Replaced static "WC 2026 countdown + 12 group flag preview" with DB-driven live + upcoming match ticker. Schematic deviation from homepage.md §1 (which spec'd 4 modes including countdown). (b) Query strategy: getTickerFixtures() runs BOTH live and upcoming queries, concatenates results with live first. Total cap of 15 fixtures combined, with live having no upper bound (more than 15 live → all live, zero upcoming). Batched (3 queries max: live JOIN, upcoming JOIN, teams hydrate). No N+1. (c) Live status codes covered: 1H, HT, 2H, ET, BT, P, LIVE. Current seeded data has only 2H present (2 stale fixtures). Other codes will appear when Phase 7 live poller runs. (d) Polling: server-side RSC fetch per page load, no client polling. Phase 7+ follow-up: TickerStrip subscribes to Pusher ticker channel for live score pushes. Until then, ticker is stale during in-page SPA navigation and refreshes only on full page navigation. (e) Auto-scroll animation: CSS-only marquee, linear timing, infinite loop, translateX(0) → translateX(-50%) with duplicated content for seamless flow. Speed: ~60 pixels/second. Pause on hover. Respects prefers-reduced-motion (animation: none when set). (f) Render-time guard filters fixtures with null/incomplete team references. Display fallback chain for team labels: code → shortName.en → first 4 chars of name.en → '???' (last resort). (g) Static LIVE indicator removed — each cell shows its own status (red pulse + minute on live cells, kickoff time on upcoming cells). Mode flag dropped from TickerStrip props; only 'fixtures' and 'empty' results in the type. (h) Competition shields render via raster competition.logoUrl from API-Football. Sized w-5 h-5, object-contain, lazy-loaded, fallback to slug abbreviation badge on null/error. Phase 7+ curated SVG shield icon set logged separately. (i) Match cells link to /[locale]/match/[id] (placeholder until Phase 8). Per deferred-affordance rule. (j) CenterTabs tab spacing polish: four tabs now distribute equally across center column width via grid grid-cols-4. (k) Phase 7+ BACKLOG logged: Pusher live ticker channel, curated SVG competition shields, 2 stale 2H fixtures (1391175, 1513400) need data ingestion fix.
- [2026-06-02][phase 8 / 9] Hydration #418 in page-specific components, deferred out of phase. LiveScoreDisplay (src/components/match/ScoreHeader.tsx → match page, Phase 8) and TeamMatchesList (equipe/[...slug] → team page, Phase 9) render timezone-naive date/time (formatMatchTime/Intl without timeZone, UTC server vs local client) without suppressHydrationWarning — same #418 class fixed across shared components in the 2026-06-02 hydration sweep. Not fixed because they are page-specific to later-phase pages. Apply the same suppressHydrationWarning fix when those phases open. The shared/cross-cutting components (TickerStrip, FeaturedMatchCard, FixtureRow, TournamentPageHeader, homepage cards, fixtures tabs) were all fixed in Phase 7.
- [2026-06-03][phase 9] Team + player pages are NOT placeholders (the 2026-05-18 "placeholders" note is stale) — both are fully built (team: header + standings + squad-by-position; player: stats table + Season/Career/Trophies tabs + info). Two perf findings from the 2026-06-03 page-load audit, deferred as page-specific Phase-9 work: (a) Team page is heavy — ~314KB payload, ~607ms response duration, LCP ~940ms (slowest of the four page types) because it bundles header + full standings + full squad into one render. Consider splitting the squad/standings into a deferred or tab-gated load. (b) Transient hydration #418 (args=HTML, structural) observed during player→player soft navigation — a player-component server/client mismatch; needs the same audit+suppress/mount-gate treatment as the Phase-7 sweep. Both are out of Phase 8 (match) scope.
- [2026-06-03][phase 8] Match page stray ?_rsc=… prefetch returned 503 (observed in 2026-06-03 audit). Did not affect the rendered page. Could be a transient Vercel edge/optimizer hiccup or a real prefetch error — worth confirming whether it reproduces (and whether it correlates with the 503s seen on image-optimizer requests in earlier audits).
- [2026-06-02][phase 7+ perf] Competition page header lift blocked by renderer coupling. All three renderers (renderCupPage, renderLeaguePage, renderGenericCupPage) derive their breadcrumb final segment + page header content from awaited cached data (seasonYear, availableSeasons, matchesCount, teamsCount, tournamentPhase). Lifting the page header above the page.tsx Suspense boundary to enable a true "static shell, dynamic body" PPR split requires splitting each renderer into a Header function + Body function — ~3× write surface. Defer until visual verification confirms the page-header flash is the perceived issue (rather than just the tab body). Discovered during 2026-06-02 route-transition-stutter fix (which shipped only the route-level loading.tsx deletion + bracket loading.tsx — the minimal-viable subset of the original plan).
- [2026-06-03][phase 8B — instant-header rollout, follow-up to 8A] 8A shipped the instant client-rendered match header: `qk.matchHeader(id)` preview cache (src/lib/match-header-preview.ts), `MatchInstantFallback` (center of match/[id]/loading.tsx), centralized `MatchLink` (seeds on pointerenter/focus/touch/pointerdown/click), migrated FixtureRow + homepage MatchRow + TickerStrip, and page.tsx seeds the header from MatchDetail for back/forward warmth. Remaining 8B work: (a) Homepage competition strip — MatchRow only receives DayFixture (no competition; it lives on the parent MatchRowGroup/CompetitionGroup). 8A omitted it (strip reserved/blank, RSC upgrades it). To show competition on homepage soft-nav, thread `competition` from MatchRowGroup → MatchRow → previewFromDayFixture(fixture, competition). (b) Migrate remaining inline match links to MatchLink: src/components/match/{H2HPanel,NextMatchCard,MatchClientSidebar}.tsx, src/components/homepage/{HomeNextMatch,HomeTodaysMatches,HomeMatchTabs,EditorialHero,cards/MatchesThisWeek}.tsx, src/components/tournament/{FeaturedMatchCard,WCFixturesTab,CupFixturesTab}.tsx, src/components/league/LeagueRightRailCard.tsx, src/components/team/TeamMatchesList.tsx (team page = Phase 9), and search results. Until migrated, those surfaces degrade to the shaped skeleton on soft-nav (correct fallback, just not instant). (c) Optional: the partial fallback intentionally omits venue/referee/round/goal-scorers (info strip space reserved → no jump); revisit only if the ~150ms upgrade reads as a flash in practice.
- [2026-06-03][phase 8B — sub-batches 1 & 2 SHIPPED] Done: (a) homepage competition strip now threaded (MatchRowGroup → MatchRow → previewFromDayFixture); (b) migrated homepage (HomeNextMatch, HomeTodaysMatches, HomeMatchTabs, EditorialHero modes A/B/C, cards/MatchesThisWeek) + tournament/league (FeaturedMatchCard, WCFixturesTab, CupFixturesTab, LeagueRightRailCard) to MatchLink. previewFromFixtureRow extended with optional minute + competition (accepts Date|string). Commits 1d1110d (sub-batch 1) + 5c59dd9 (sub-batch 2). STILL REMAINING (sub-batch 3, DEFERRED per decision — weak data, keep skeleton fallback): src/components/match/H2HPanel.tsx (only team IDs + name strings at link site, no team objects/logos → partial header would be low quality) and src/components/match/NextMatchCard.tsx (opponent name only, no scores). To migrate either, thread full team objects from their parents first. Also: the 8B sweep found NO search component linking to /match/ (search links to entities), so nothing to migrate there. TeamMatchesList stays Phase 9 (team page).
- [2026-06-03][phase 10 — T7 deferred half] Typography floor (mobile readability) DEFERRED from Phase-10 Task 7. ~93 occurrences of text-[10px] (57) / text-[11px] (36) across ~50 files; many are intentional density (standings/stats tables, brackets, badges). A blanket bump to a ≥12px content floor risks layout overflow/wrapping that can't be verified without a real mobile viewport, and curating 93 spots is high-judgment/low-confidence. T7 shipped only the touch-target fixes (carousel arrows p-2→p-3, ticker pause w-8→w-11, match nav tabs py-3→py-3.5). To revisit the type floor: do it as a curated, per-component pass (start with primary reading content actually surfaced on mobile — score-header goal scorers, homepage rail widgets, match event/stats rows), verifying each on a 375–390px viewport; NOT a global find-replace. Top offender files: TeamMatchesList(6×10px), LeagueRightRail(5), HomeNextMatch(6×11px), TeamStatistics(4×11px), TeamPageHeader(4), HomeLionsAbroad(4).

- [2026-06-04][phase 13 — data completeness, RESOLVED] Player photos: 8,114 players (~26%) had null photo_url despite the CDN holding the image — backfilled to 0 missing via scripts/backfill-player-photos.ts (free HEAD checks against media.api-sports.io, sets URL only on 200, no broken images). Players now 100% photo coverage (31,464). Fixture venues: scripts/backfill-venues.ts re-fetched 4,918 finished (FT/AET/PEN) null-venue fixtures by id (246 API calls) — only 52 recoverable; the remaining ~4,866 return `venue: null` from API-Football itself (old/lower-tier fixtures the provider never had a venue for). This is an UPSTREAM data limitation, not an ingestion bug — do not re-investigate. Both scripts committed b7cda86.

- [2026-06-04][phase 9 — team page mobile overhaul, SHIPPED] Mobile team page reworked: hero redesigned (Variant B — full-height logo left, name/form/dense meta line right; fact pills now desktop-only md:flex); breadcrumb compact on mobile via new opt-in SeoBreadcrumb `compactOnMobile` prop (JSON-LD untouched, other callers unaffected); added a **Matches** center tab (first/default, content = TeamMatchesList) and renamed the Players tab → **Squad** (labelKey 'players'→'squad', both breakpoints, no i18n changes — keys already existed); mobile Next/Last band (FeaturedMatchCard) now sits above the tabs (xl:hidden) instead of below. OPEN DECISION (deferred, user chose "keep both"): desktop now shows the full matches list in BOTH the new center Matches tab AND the right-rail TeamMatchesList. If the desktop duplication reads poorly, drop the right-rail list (page.tsx `rightRail` prop) — kept for now per explicit choice. Files: TeamPageHeader.tsx, equipe/[...slug]/page.tsx, chrome/SeoBreadcrumb.tsx. tsc + build clean.

- [2026-06-04][phase 9 — team page polish, SHIPPED] Breadcrumb expanded on mobile (SeoBreadcrumb no longer collapses middle crumbs when compactOnMobile; ~2px padding); national-team competition crumb added via fallback to the default standings competition (getTeamPrimaryCompetition is League-only → null for Morocco; allStandings.competitions[0] = World Cup 2026, rendered as a text-only crumb since CompetitionSnapshot has no slug/countryCode → no link). SEO <title> now "{Team} — {seoSections} | DimaScore" using new teamPage.seoSections key (en/fr/ar). Tab icons removed (Matches calendar dropped → text-only tab bar). Files: chrome/SeoBreadcrumb.tsx, equipe/[...slug]/page.tsx, i18n ×3. tsc + build clean.
- [2026-06-04][phase 9 — BLOCKED, data] Requested: WC2026 Squad tab should list only the 26-man tournament selection, not the full national pool. NOT buildable with current data — squad_members has only (team_id, player_id); API-Football's squad feed returns the full pool, and the official WC2026 roster isn't published/ingested yet. To do later: either (a) wait for the named roster to land in the squad feed near the tournament, or (b) add a curated call-up table + admin UI to flag tournament selections. Until then the Squad tab shows the full squad.
- [2026-06-04][phase 9 — QUEUED] Standings tab: show ONLY the highlighted team's own group first (Morocco = Group C) with a "view full standings" expander to reveal all groups. Feasible in TeamStandingsSection/TeamStandingsWithFilter (StandingRow has groupLabel; highlightTeamId known). Needs its own propose-then-implement pass.

- [2026-06-04][phase 9 — standings collapse, SHIPPED] Task B done: team page standings now collapse to the highlighted team's own group by default (e.g. Morocco → Group C) with a centered "View full standings" ⇄ "Show less" toggle. Implemented in TeamStandingsWithFilter (showAllGroups state; canCollapse = groupCount>1 && team is in standings; resets on competition/season switch). Single-group leagues unchanged (no toggle). New i18n teamPage.viewFullStandings + teamPage.showLess (en/fr/ar). tsc + build clean. (Supersedes the QUEUED note above.)

- [2026-06-04][phase 9 — WC2026 curated squad, SHIPPED] tournament_squads table (competition_id, season_year, team_id, player_id) + migration 0004 applied; getTeamTournamentSquad query; Squad tab on the team page now shows the curated roster (full pool hidden) with a competition-name caption when rows exist, else the normal squad. Seeded Morocco WC2026 = 26 players (comp 1, season 2026, team 31). All 26 matched to player_ids (22 from national pool; Tagnaouti 2703 / Bouaddi 438688 / Amaimouni 535046 found under clubs; "Munir El Kajoui" = id 2702 "M. Mohamedi", confirmed via Wikipedia birth-name). Rename of 2702 was DECLINED (API serves "M. Mohamedi"; weekly squads cron would revert any manual name; abbreviated form is consistent with the rest of the squad). To add more national teams: insert their 26 into tournament_squads (comp 1, season 2026) — Squad tab auto-switches.
- [2026-06-04][phase 9 — deferred] Display-name override mechanism: if we ever want durable custom player names (e.g. "Munir El Kajoui" instead of API's "M. Mohamedi"), add a column the squads-refresh ingestion respects (it currently overwrites players.name from API every Sunday). General feature, not worth special-casing one player.
- [2026-06-04][phase 9 — minor] migration 0004 PK constraint name exceeded Postgres' 63-char limit and was truncated to "tournament_squads_competition_id_season_year_team_id_player_id_" (harmless NOTICE 42622 on apply). A future drizzle-kit generate may emit a no-op rename diff for it; if so, shorten the PK constraint name in schema.ts.

- [2026-06-05][phase 9 — WC2026 bulk squads] Resolver pipeline built: scripts/data/wc2026-squads.ts (FIFA squads + dbName aliases), scripts/data/wc2026-overrides.ts (manual name→id pins), scripts/lib-wc2026.ts (accent-insensitive in-memory matcher: token overlap + adjacent-pair concat for hyphenated romanizations + given-name initial tiebreak; pool→global→ambiguous→missing), scripts/resolve-wc2026-squads.ts (read-only report), scripts/seed-wc2026-squads.ts (seeds matched only), scripts/ingest-wc2026-players.ts (insert genuinely-missing players). Group A done: Mexico 26/26, Czech Republic 26/26, South Korea 25/26. Ingested Edson Álvarez #2869 (distinct from Efraín #51068) + David Doudera #66214 (API "Douděra"). Pins: Santiago Giménez 94562, David Zima 128772, Adam Hložek 66019. RUN ORDER: ingest-wc2026-players → seed-wc2026-squads (FK). South Africa omitted (squad unpublished → DB fallback). Scoped plan: only curate Morocco's group + marquee teams, not all 48.
- [2026-06-05][phase 9 — MISSING] Cho Wije (South Korea WC2026 DEF, FIFA #?) not located in API-Football: absent from Korea's /players?team=17 season list (fresh call-up) and not on profiles?search=cho p1. South Korea seeds 25/26 until found. To resolve: search API-Football by club once known, or wait for him to appear in Korea's squad feed, then add to ingest list + overrides.

- [2026-06-05][phase 9 — RESOLVED] Cho Wije (South Korea) now in API-Football squad feed (team 17) as #547307 "Cho Wi-Je" (Defender). Added to ingest-wc2026-players.ts + pinned in overrides ('South Korea' → 'Cho Wije': 547307). Resolver: Korea 26/26, Group A 78/78. RUN: ingest-wc2026-players → seed-wc2026-squads. Supersedes the earlier MISSING note.

- [2026-06-05][phase 9 — WC2026 squads, SIMPLIFIED + CLEANUP] API-Football now returns the finalized 26-man squad per WC team, so the whole FIFA-list/resolver/overrides pipeline is obsolete. Replaced by a single direct seeder: scripts/seed-wc2026-squads-from-api.ts (pulls all 48 teams' squads by team id, upserts players without touching currentTeamId, replaces each team's tournament_squads with the API's 26). DELETED: scripts/data/wc2026-squads.ts, scripts/data/wc2026-overrides.ts, scripts/lib-wc2026.ts, scripts/resolve-wc2026-squads.ts, scripts/seed-wc2026-squads.ts, scripts/ingest-wc2026-players.ts, scripts/seed-tournament-squad-morocco-wc2026.ts. (Supersedes all earlier "bulk squads" / Cho Wije backlog notes.)

- [2026-06-06][phase 13 — audit P0-6 follow-ups, DEFERRED] Two lower-risk follow-ups split off from the getCompactTeamLabel/tri-code fix (logic branch + scripts/seed-fifa-tricodes.ts shipped): (1) cryptic CLUB codes (Algerian Ligue 1 / Botola 2: ORA, MOG, CAK, JEU, CODM…) are still in teams.code — now harmless in the ticker (club branch prefers shortName/name), but consider nulling the unrecognisable ones in a data pass. (2) Ticker "OD vs HAS" secondary bug: one team renders an empty home slot; tighten TickerStrip isRenderable to require BOTH teams to have a non-empty label (or drop the row). See audit docs/audits/2026-06-06-prelaunch-audit.md P0-6.

- [2026-06-06][phase 13 — audit P0-1, NEEDS REAL CONTENT before launch] Footer pages (legal/privacy/about/contact/faq) shipped with starter copy in src/lib/constants/site-pages-content.ts. Placeholders to replace: (1) CONTACT_EMAIL = 'contact@dimascore.com' (swap for the real inbox). (2) Legal notice — publisher legal entity / name / address / registration ([TODO] in legal.sections). (3) Hosting line — confirm host name + address. (4) Privacy — data-controller identity + CNDP declaration reference ([TODO] in privacy 'Your rights'). FR/AR copy is concise machine-quality starter text — have a native speaker review before launch. Grep the files for 'TODO'.

- [2026-06-06][phase 13 — audit P1-5, DATA BACKFILL deferred] WC2026 edition selector lists 2026/2022/2018/2010 but skips 2014 (in seasons table, 0 fixtures/0 standings) and 2006 (no season row), while the hardcoded Titles honor-roll shows both. Root cause: getAvailableSeasons (league.ts:128) filters to isCurrent OR has-fixtures, and historical WC editions have little/no ingested data (2018=4 fixtures, 2010=2, 2014=0). Not a selector bug — a data-completeness gap. No clean filter fix (requiring standings would correctly hide empties but break knockout cups, which have no standings). Proper fix = backfill historical WC editions (2014/2010/2018/2006 standings + fixtures) post-launch; then they appear automatically. Audit P1-5.

- [2026-06-06][phase 13 — LIVE-DATA FRESHNESS, benchmarked vs SofaScore/FlashScore/LiveScore] Current architecture: api-sports → Railway live-poller (15s live / single upstream subscriber) → DB → /api/v1/live → client poll (useLiveFixtures: 30s live, 60s idle, tab-hidden paused) + Pusher demoted to refetch-nudge. Competitors hit sub-15s: SofaScore = self-hosted WebSocket push; FlashScore/LiveScore = short-interval gzipped DELTA feed in a Web/Service Worker. Key insight: we're a TWO-STAGE pipeline so the freshness floor is the worker's upstream cadence (quota-bound), not the client transport; and competitors OWN their feed while we RENT a metered one — switching client transport does NOT change api-sports spend. Their worker/SW/delta machinery exists for global all-sport thousands-of-matches scale we don't have.
  DO NOW — ✅ ALL DONE [2026-06-06]: (1) ✅ /api/v1/live now sends ETag + If-None-Match 304 and Cache-Control public, max-age=0, s-maxage=5, stale-while-revalidate=30 (5s shared/CDN cache collapses concurrent DB reads to ~1 per 5s/region; browser revalidates so never stale). (2) ✅ Featured-card poll wired — and extended beyond HomeFeaturedCarousel to FeaturedMatchCard (team/player/competition right-rail) and HomeNextMatch (homepage rail + maroc edition); all flip live↔finished via the shared useLiveFixtures poll. NextMatchCard (next opponent only) and EditorialHero (dead code) left as-is. (3) ✅ live client interval tightened 30s→15s, and useLiveFixtures fetch drops cache:no-store so the browser honours the ETag/cache. Net freshness now ~15s client + ≤5s CDN + ≤15s worker; the worker's 15s upstream cadence remains the floor.
  MAYBE LATER (selective): nudge the worker's api-sports cadence to ~8-10s for marquee live matches (Atlas Lions) — this is the lever that actually spends quota.
  DEFER / DON'T CHASE YET: self-hosted WebSocket à la SofaScore (~weeks + a permanent always-on socket fleet + ops; Vercel serverless can't hold sockets); Web Worker / Service Worker delta engine à la FlashScore/LiveScore (~3-4 weeks + SW maintenance; solves main-thread jank we don't have at our tiny payload); Pusher-PRIMARY (we're ~80% there: publish full score+minute every worker tick instead of only deltas, ~2-4 days, but mainly buys latency not cost, and Pusher bill scales with concurrent connections). Recommendation: take the DO-NOW slice, re-evaluate push only if a real matchday proves sub-15s is needed.

- [][phase 13 — knockout bracket node] Live ties show "LIVE" not the match minute (e.g. 67') because `FixtureWithTeams`/`BracketMatch` carry no minute/elapsed field. To show the minute in `BracketCardNode`, add an `elapsed`/`minute` field through `getKnockoutFixtures` → the `Tie`/`BracketMatch` build → the node. Invisible on past editions (no live matches); matters only for a live AFCON knockout.

- [phase 13 — standings] Duplicate/messy group_label rows in `standings`: WC 2026 has both canonical `Group A…Group L` AND `Group Stage`/`Group Stage - Group A…` variants (108 rows for 48 teams). The standings sync (`syncStandings` / API-Football response) is creating inconsistent group labels, which can surface as duplicate group tables. Clean up: normalize group_label on ingest (strip "Group Stage - " prefix), and/or de-dupe existing rows. Separate from the compute-from-fixtures fix.

- [phase 13 — live poller] Proper fix for finished matches freezing at "2H 90'": the Railway live poller only writes fixtures the live feed returns, so when a match ends it drops off the feed and is never written to FT. `finalize-stale` (now gated on `updated_at` staleness, ~5 min) is the in-repo safety net, but it still leaves a finished match looking live for several minutes. The real fix is for the poller to detect a fixture that was live last cycle and is now absent from the feed, and re-fetch+finalize it immediately. Poller code is external (Railway), not in this repo.

- [phase 13 — status sets] `FINISHED_*` drift (separate from the live-set consolidation, which is now done via `LIVE_CODES_ARRAY`). The "finished" set still varies by call site: `['FT','AET','PEN']` (top-matches, compute, HomeLiveGroupStandings) vs `['FT','AET','PEN','WO','AWD']` (right-rail, homepage) vs canonical `FINISHED_CODES_ARRAY = ['FT','AET','PEN','WO','AWD','CANC','ABD']`. So WO/AWD/CANC/ABD count as "finished" in some surfaces and not others. Some variance is intentional (standings only score FT/AET/PEN), but the query-layer ones should likely import `FINISHED_CODES_ARRAY`. Also: `TERMINAL_STATUSES` in render-cup-page/TickerStrip and `TERMINAL_CODES` in MatchLiveUpdater are yet another axis (include PST/SUSP/INT inconsistently) — audit when touching finished-state logic. Also `src/lib/db/schema.ts:268` has a partial-index predicate `status_code IN ('1H','HT','2H','ET','BT','P','LIVE')` (no INT) — a raw SQL literal in a Drizzle index; aligning it to the canonical live set needs a migration, so it was left. Minor: an INT-status query just won't use that partial index (perf, not correctness).

- [2026-07-03][phase 14 — feed-authoritative guard, remaining surfaces] The absent-from-feed guard (server-"live" fixture with no patch in a successful `/api/v1/live` payload → treat as finished) was added to HomeNextMatch only. Same fallthrough exists in HomeFeaturedCarousel, TickerStrip, and MatchLiveUpdater — each falls back to stale server props when a fixture drops out of the feed (>4h after FT). Reuse `useLiveFeedReady()` from useLiveFixtures.

- [2026-07-03][phase 14 — timezone/hydration P0] Kickoff times render in UTC server-side and viewer-local client-side → React error #418 (hydration text mismatch) + times shown 1h early pre-hydration and permanently on non-hydrating surfaces (ticker, Top Matches, match header, competition pages), while "Today" list shows correct local time on the same page. Fix: shared `<LocalTime>` client component (epoch in, formats after mount, suppressHydrationWarning) used by every kickoff surface. Recommended next task — confirmed live on dimascore.ma and by the 2026-07-03 functional audit.

- [2026-07-03][phase 14 — ISR staleness] Production homepage serves `x-vercel-cache: STALE` with `age` up to ~22 min despite `cacheLife('minutes')` (measured on /en and /fr, 2026-07-03). Audit also caught cross-load drift: Botola standings round 28 vs 29, top-scorer #5 differing between loads. Check Vercel logs for failing background revalidations; consider `revalidateTag` fired from sync crons instead of pure time-based expiry.

- [2026-07-03][phase 14 — homepage standings widget] Botola mini-standings omits the draws column (shows W/L only — "15 3" hides D=11). Show the full record.

- [2026-07-03][phase 14 — i18n gaps] AR/FR pages keep some strings in English (competition names like "WORLD CUP · GROUP E", "Bundesliga"; assorted labels), and AR showed a UTC time on one surface. Sweep untranslated strings after the timezone fix lands.

- [2026-07-03][phase 14 — deep-link repro] Audit hit one direct navigation to /match/{id} that redirected to the homepage (clicking the same row worked). Possibly transient; needs a repro attempt on cold deep-links before it's actionable.

- [2026-07-03][phase 14 — footer copy] Footer claims "Live data — updated every 15s" — only true while the tab is visible AND a match is live (60s idle cadence, paused when hidden). Either soften the copy or leave it once focus-refetch ships and the claim is effectively honest.

- [2026-07-03][phase 14 — Pusher reconnect] On Pusher reconnect after a disconnect (network blip, page freeze), missed events are not replayed and nothing forces a catch-up. Low priority now that refetchOnWindowFocus:'always' covers refocus; a `connected`-state handler that invalidates `['live-fixtures']` would close the visible-tab network-blip window.
