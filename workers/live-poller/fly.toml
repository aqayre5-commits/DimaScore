app = "dimascore-live-poller"
primary_region = "cdg"

[build]
  dockerfile = "workers/live-poller/Dockerfile"

[processes]
  worker = "tsx src/index.ts"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  processes = ["worker"]
